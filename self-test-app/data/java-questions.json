[
  {
    "id": 1,
    "category": "Java Platform",
    "question": "Why is Java so popular?",
    "options": [
      "1. Platform Independence: Java programs can run on any device that has the Java Virtual Machine (JVM), making it highly portable.",
      "One misleading statement is that platform independence means every Java UI looks identical on all systems without any adaptation, even though the runtime model says otherwise.",
      "A frequent but false claim is that Java applications normally run without a JVM because modern operating systems execute bytecode directly, which does not match how Java actually behaves.",
      "Another wrong assumption is that Java became popular mainly because it avoids garbage collection entirely, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "1. Platform Independence: Java programs can run on any device that has the Java Virtual Machine (JVM), making it highly portable."
  },
  {
    "id": 2,
    "category": "Java Platform",
    "question": "What is platform independence?",
    "options": [
      "One misleading statement is that the JRE includes every compiler and packaging tool, while the JDK is only for running programs, but that would misrepresent the real contract of the feature.",
      "A frequent but false claim is that platform independence means every Java UI looks identical on all systems without any adaptation, but the official semantics are different.",
      "Another wrong assumption is that bytecode is just processor-specific machine code with a different file extension, which conflicts with the usual rules of the language.",
      "Platform independence refers to the ability of a programming language or software to run on various types of computer systems without modification."
    ],
    "answer": "d",
    "explanation": "Platform independence refers to the ability of a programming language or software to run on various types of computer systems without modification. In the context of Java, platform independence is achieved through the use of the Java"
  },
  {
    "id": 3,
    "category": "Java Platform",
    "question": "What is bytecode?",
    "options": [
      "A frequent but false claim is that Java programs can run only on desktop machines and not on servers or embedded devices, but that description mixes up separate parts of the platform.",
      "Another wrong assumption is that the JRE includes every compiler and packaging tool, while the JDK is only for running programs, even though the language and APIs work another way.",
      "Bytecode is an intermediate representation of a Java program. When a Java program is compiled, it is converted into bytecode, which is a set of instructions that can be executed by the Java Virtual Machine (JVM).",
      "A tempting distractor is to believe that a `.class` file stores the original source text and comments for direct execution, but the Java platform defines it differently."
    ],
    "answer": "c",
    "explanation": "Bytecode is an intermediate representation of a Java program. When a Java program is compiled, it is converted into bytecode, which is a set of instructions that can be executed by the Java Virtual Machine (JVM). Bytecode is"
  },
  {
    "id": 4,
    "category": "Java Platform",
    "question": "Compare JDK vs JVM vs JRE",
    "options": [
      "an interpreter/loader (Java), a compiler (javac), an archiver (jar), a documentation generator (Javadoc), and other tools needed for Java development.",
      "An inaccurate explanation would say that the JVM is the full SDK used to write, compile, and document applications, even in modern Java releases.",
      "A tempting distractor is to believe that Java programs can run only on desktop machines and not on servers or embedded devices, which would lead to the wrong design or interview conclusion.",
      "A mistaken idea is that portability in Java comes from avoiding any runtime layer between the code and the hardware, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "an interpreter/loader (Java), a compiler (javac), an archiver (jar), a documentation generator (Javadoc), and other tools needed for Java development."
  },
  {
    "id": 5,
    "category": "Java Platform",
    "question": "What are the important differences between C++ and Java?",
    "options": [
      "C++ need to be compiled for each platform, Java is platform independent.",
      "A tempting distractor is to believe that Java source code must be rebuilt into a separate native executable for every operating system, even though the runtime model says otherwise.",
      "A mistaken idea is that the JVM is the full SDK used to write, compile, and document applications, which does not match how Java actually behaves.",
      "One incorrect narrative is that C++ and Java follow the same portability and deployment model, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "C++ needs to be compiled for each platform, while Java is platform-independent and compiled to bytecode that runs on any JVM."
  },
  {
    "id": 7,
    "category": "Wrapper Classes",
    "question": "What are Wrapper classes?",
    "options": [
      "A mistaken idea is that `Integer.valueOf()` always creates a brand-new object and never reuses cached instances, but that description mixes up separate parts of the platform.",
      "Wrapper classes in Java are classes that encapsulate a primitive data type into an object. They provide a way to use primitive data types (like `int`, `char`, etc.) as objects.",
      "A flawed answer would argue that unboxing is available only inside arithmetic expressions, which is not how production Java code is expected to work.",
      "An easy trap is to assume that boxing and casting are interchangeable names for the same operation, but the Java platform defines it differently."
    ],
    "answer": "b",
    "explanation": "Wrapper classes in Java are classes that encapsulate a primitive data type into an object. They provide a way to use primitive data types (like `int`, `char`, etc.) as objects. Each primitive type has a corresponding wrapper class:"
  },
  {
    "id": 8,
    "category": "Wrapper Classes",
    "question": "Why do we need Wrapper classes in Java?",
    "options": [
      "One incorrect narrative is that wrapper classes exist only for backward compatibility and have no role in modern Java, but the Java platform defines it differently.",
      "A flawed answer would argue that `Integer.valueOf()` always creates a brand-new object and never reuses cached instances, even in modern Java releases.",
      "Wrapper classes in Java are needed for the following reasons:",
      "A false summary would state that `null` can always be unboxed safely into the default primitive value, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "c",
    "explanation": "Wrapper classes in Java are needed for the following reasons: enabling them to be used in object-oriented programming contexts."
  },
  {
    "id": 9,
    "category": "Wrapper Classes",
    "question": "What are the different ways of creating Wrapper class instances?",
    "options": [
      "A flawed answer would argue that Java forbids automatic conversion between a primitive and its wrapper, but the JVM and standard libraries do not operate like that.",
      "An easy trap is to assume that wrapper classes exist only for backward compatibility and have no role in modern Java, even though the runtime model says otherwise.",
      "There are two main ways to create instances of Wrapper classes in Java:",
      "A common misconception is that wrapper classes are mutable, so changing one reference updates every equal value, but that would misrepresent the real contract of the feature."
    ],
    "answer": "c",
    "explanation": "There are two main ways to create instances of Wrapper classes in Java: Each wrapper class has a constructor that takes a primitive type or a String as an argument."
  },
  {
    "id": 10,
    "category": "Wrapper Classes",
    "question": "What are the differences in the two ways of creating Wrapper classes?",
    "options": [
      "The two ways of creating Wrapper class instances in Java are using constructors and using static factory methods.",
      "A false summary would state that autoboxing works only for numeric primitives and not for `boolean` or `char`, but the official semantics are different.",
      "A common misconception is that Java forbids automatic conversion between a primitive and its wrapper, which conflicts with the usual rules of the language.",
      "An incorrect interpretation is that wrapper objects always consume less memory than primitives, but that description mixes up separate parts of the platform."
    ],
    "answer": "a",
    "explanation": "The two ways of creating Wrapper class instances in Java are using constructors and using static factory methods. Here are the differences:"
  },
  {
    "id": 11,
    "category": "Wrapper Classes",
    "question": "What is autoboxing?",
    "options": [
      "A false summary would state that wrapper classes inherit directly from their matching primitive types, but that description mixes up separate parts of the platform.",
      "Autoboxing in Java is the automatic conversion that the Java compiler makes between the primitive types and their corresponding object wrapper classes.",
      "An incorrect interpretation is that autoboxing works only for numeric primitives and not for `boolean` or `char`, which is not how production Java code is expected to work.",
      "One misleading statement is that methods such as `Integer.parseInt` return wrapper objects instead of primitives, but the Java platform defines it differently."
    ],
    "answer": "b",
    "explanation": "Autoboxing in Java is the automatic conversion that the Java compiler makes between the primitive types and their corresponding object wrapper classes. For example, converting an `int` to an `Integer`, a `double` to a `Double`, and"
  },
  {
    "id": 12,
    "category": "Wrapper Classes",
    "question": "What are the advantages of autoboxing?",
    "options": [
      "Autoboxing in Java provides several advantages:",
      "An incorrect interpretation is that boxing and casting are interchangeable names for the same operation, even in modern Java releases.",
      "One misleading statement is that wrapper classes inherit directly from their matching primitive types, which would lead to the wrong design or interview conclusion.",
      "A frequent but false claim is that wrapper instances cannot be stored in collections because collections accept only primitives, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "Autoboxing in Java provides several advantages: such as collections."
  },
  {
    "id": 13,
    "category": "Wrapper Classes",
    "question": "What is casting?",
    "options": [
      "Casting in Java is the process of converting a value of one data type to another. There are two types of casting:",
      "One misleading statement is that `null` can always be unboxed safely into the default primitive value, even though the runtime model says otherwise.",
      "A frequent but false claim is that boxing and casting are interchangeable names for the same operation, which does not match how Java actually behaves.",
      "Another wrong assumption is that unboxing is available only inside arithmetic expressions, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "Casting in Java is the process of converting a value of one data type to another. There are two types of casting: implicit casting (automatic) and explicit casting (manual)."
  },
  {
    "id": 14,
    "category": "Wrapper Classes",
    "question": "What is implicit casting?",
    "options": [
      "One misleading statement is that wrapper classes are mutable, so changing one reference updates every equal value, but that would misrepresent the real contract of the feature.",
      "A frequent but false claim is that `null` can always be unboxed safely into the default primitive value, but the official semantics are different.",
      "Implicit casting in Java is the automatic conversion of a smaller data type to a larger data type. Java performs implicit casting when the target data type can accommodate the source data type without loss of precision.",
      "An inaccurate explanation would say that `Integer.valueOf()` always creates a brand-new object and never reuses cached instances, but that description mixes up separate parts of the platform."
    ],
    "answer": "c",
    "explanation": "Implicit casting in Java is the automatic conversion of a smaller data type to a larger data type. Java performs implicit casting when the target data type can accommodate the source data type without loss of precision. For"
  },
  {
    "id": 15,
    "category": "Wrapper Classes",
    "question": "What is explicit casting?",
    "options": [
      "A frequent but false claim is that wrapper objects always consume less memory than primitives, but that description mixes up separate parts of the platform.",
      "Another wrong assumption is that wrapper classes are mutable, so changing one reference updates every equal value, even though the language and APIs work another way.",
      "Explicit casting in Java is the manual conversion of a larger data type to a smaller data type, or when converting between incompatible types.",
      "A tempting distractor is to believe that wrapper classes exist only for backward compatibility and have no role in modern Java, but the Java platform defines it differently."
    ],
    "answer": "c",
    "explanation": "Explicit casting in Java is the manual conversion of a larger data type to a smaller data type, or when converting between incompatible types. Java requires explicit casting when the target data type may lose precision or range"
  },
  {
    "id": 16,
    "category": "Strings",
    "question": "Are all String objects immutable?",
    "options": [
      "String’s immutable?\\ No, not all `String` objects are immutable.",
      "An inaccurate explanation would say that modern Java forbids text concatenation with the `+` operator, even in modern Java releases.",
      "A tempting distractor is to believe that every identical string literal creates a brand-new object even when the pool already contains it, which would lead to the wrong design or interview conclusion.",
      "A mistaken idea is that `String` equality uses reference comparison only and cannot be customized, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "String’s immutable?\\ No, not all `String` objects are immutable. In Java, `String` objects are immutable, meaning once a `String` object is created, its value cannot be changed. However, there are other classes like `StringBuilder` and `StringBuffer` that provide mutable alternatives to `String`."
  },
  {
    "id": 17,
    "category": "Strings",
    "question": "Where are String values stored in memory?",
    "options": [
      "An inaccurate explanation would say that `StringBuffer` is always faster than `StringBuilder` because synchronization has no overhead, but the JVM and standard libraries do not operate like that.",
      "A tempting distractor is to believe that modern Java forbids text concatenation with the `+` operator, even though the runtime model says otherwise.",
      "In Java, `String` values are stored in a special memory area called the String Pool. The String Pool is part of the Java heap memory and is used to store unique `String` literals.",
      "One incorrect narrative is that the String Pool stores only user input and does not apply to literals in code, but that would misrepresent the real contract of the feature."
    ],
    "answer": "c",
    "explanation": "In Java, `String` values are stored in a special memory area called the String Pool. The String Pool is part of the Java heap memory and is used to store unique `String` literals. When a `String` literal is created, the JVM checks"
  },
  {
    "id": 18,
    "category": "Strings",
    "question": "Why should you be careful about String concatenation (+) operator in loops?",
    "options": [
      "A tempting distractor is to believe that `StringBuilder` is immutable, while `String` is the mutable text type, but that would misrepresent the real contract of the feature.",
      "A mistaken idea is that `StringBuffer` is always faster than `StringBuilder` because synchronization has no overhead, but the official semantics are different.",
      "One incorrect narrative is that `String` objects are mutable and update in place during concatenation, which conflicts with the usual rules of the language.",
      "String concatenation using the `+` operator in loops can be inefficient due to the immutability of `String` objects."
    ],
    "answer": "d",
    "explanation": "String concatenation using the `+` operator in loops can be inefficient due to the immutability of `String` objects. Each time a `String` is concatenated using the `+` operator, a new `String` object is created, resulting in"
  },
  {
    "id": 19,
    "category": "Strings",
    "question": "How do you solve the problem of inefficient string concatenation in loops?",
    "options": [
      "A mistaken idea is that calling `intern()` moves text out of the JVM and into operating-system memory only, but that description mixes up separate parts of the platform.",
      "One incorrect narrative is that `StringBuilder` is immutable, while `String` is the mutable text type, even though the language and APIs work another way.",
      "A flawed answer would argue that the `String` class has almost no utility methods beyond `length()`, which is not how production Java code is expected to work.",
      "To solve the problem of inefficient string concatenation using the `+` operator in loops, you should use `StringBuilder` or `StringBuffer`."
    ],
    "answer": "d",
    "explanation": "To solve the problem of inefficient string concatenation using the `+` operator in loops, you should use `StringBuilder` or `StringBuffer`. These classes provide mutable alternatives to `String` and are more efficient for concatenation operations, especially in loops. `StringBuilder` is generally preferred over `StringBuffer` when thread safety is not a concern, as it offers better performance."
  },
  {
    "id": 20,
    "category": "Strings",
    "question": "What are the differences between `String` and `StringBuffer`?",
    "options": [
      "One incorrect narrative is that using `+` in a loop reuses one internal buffer automatically, so there is no allocation cost, but the Java platform defines it differently.",
      "A flawed answer would argue that calling `intern()` moves text out of the JVM and into operating-system memory only, even in modern Java releases.",
      "`StringBuffer` objects are mutable, allowing their values to be modified. But `String` objects are immutable, meaning their values cannot be changed after they are created.",
      "A false summary would state that string literals live only on the thread stack and never in heap-managed memory, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "c",
    "explanation": "`StringBuffer` objects are mutable, allowing their values to be modified. `StringBuilder` is faster for concatenation and modification operations."
  },
  {
    "id": 22,
    "category": "Strings",
    "question": "Can you give examples of different utility methods in String class?",
    "options": [
      "An easy trap is to assume that the String Pool stores only user input and does not apply to literals in code, but that would misrepresent the real contract of the feature.",
      "The `String` class in Java provides various utility methods. Here are some examples:",
      "A common misconception is that `String` equality uses reference comparison only and cannot be customized, which conflicts with the usual rules of the language.",
      "An incorrect interpretation is that every identical string literal creates a brand-new object even when the pool already contains it, but that description mixes up separate parts of the platform."
    ],
    "answer": "b",
    "explanation": "The `String` class in Java provides various utility methods. Here are some examples: // Example of length() method"
  },
  {
    "id": 23,
    "category": "Strings",
    "question": "What is a class?",
    "options": [
      "A false summary would state that `Set` keeps all duplicates in insertion order by design, but that description mixes up separate parts of the platform.",
      "A common misconception is that iterators can modify collections in any way without following any rules, even though the language and APIs work another way.",
      "A class in Java is a blueprint for creating objects. It defines a set of properties (fields) and methods that the created objects will have. A class encapsulates data for the object and methods to manipulate that data.",
      "One misleading statement is that generics in collections remove the need for compile-time type checks, but the Java platform defines it differently."
    ],
    "answer": "c",
    "explanation": "A class in Java is a blueprint for creating objects. It defines a set of properties (fields) and methods that the created objects will have. A class encapsulates data for the object and methods to manipulate that data. It serves as"
  },
  {
    "id": 25,
    "category": "Strings",
    "question": "What is the state of an object?",
    "options": [
      "An incorrect interpretation is that behavior and state are interchangeable terms in Java object modeling, but the JVM and standard libraries do not operate like that.",
      "One misleading statement is that method overriding allows a method to become a field with the same name, even though the runtime model says otherwise.",
      "The state of an object in Java refers to the data or values stored in its fields (instance variables) at any given time.",
      "Another wrong assumption is that an object's state is determined only by its method names and not by field values, but that would misrepresent the real contract of the feature."
    ],
    "answer": "c",
    "explanation": "The state of an object in Java refers to the data or values stored in its fields (instance variables) at any given time. The state represents the current condition of the object, which can change over time as the values of its fields"
  },
  {
    "id": 26,
    "category": "Object-Oriented Programming",
    "question": "What is the behavior of an object?",
    "options": [
      "One misleading statement is that inheritance removes the need for constructors in subclasses, but that would misrepresent the real contract of the feature.",
      "The behavior of an object in Java refers to the actions or operations that the object can perform. These behaviors are defined by the methods in the object's class.",
      "Another wrong assumption is that a superclass reference can never point to a subclass instance, which conflicts with the usual rules of the language.",
      "An inaccurate explanation would say that `hashCode` must always be unique for every object ever created, but that description mixes up separate parts of the platform."
    ],
    "answer": "b",
    "explanation": "The behavior of an object in Java refers to the actions or operations that the object can perform. These behaviors are defined by the methods in the object's class. The methods manipulate the object's state and can interact with other"
  },
  {
    "id": 28,
    "category": "Object-Oriented Programming",
    "question": "Explain about the toString method.",
    "options": [
      "The `toString` method in Java is a method that returns a string representation of an object. It is defined in the `Object` class, which is the superclass of all Java classes.",
      "An inaccurate explanation would say that the `String` class has almost no utility methods beyond `length()`, even in modern Java releases.",
      "A tempting distractor is to believe that `String` objects are mutable and update in place during concatenation, which would lead to the wrong design or interview conclusion.",
      "A mistaken idea is that `StringBuffer` is always faster than `StringBuilder` because synchronization has no overhead, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "The `toString` method in Java is a method that returns a string representation of an object. It is defined in the `Object` class, which is the superclass of all Java classes. By default, the `toString` method returns a string"
  },
  {
    "id": 29,
    "category": "Object-Oriented Programming",
    "question": "What is the use of equals method in Java?",
    "options": [
      "An inaccurate explanation would say that a class implements another class and extends an interface, but the JVM and standard libraries do not operate like that.",
      "A tempting distractor is to believe that abstract classes can be instantiated directly as long as their fields are initialized, even though the runtime model says otherwise.",
      "The `equals` method in Java is used to compare the equality of two objects. It is defined in the `Object` class and can be overridden in subclasses to provide custom equality logic.",
      "One incorrect narrative is that interfaces can declare constructors and instance fields just like normal classes, but that would misrepresent the real contract of the feature."
    ],
    "answer": "c",
    "explanation": "The `equals` method in Java is used to compare the equality of two objects. It is defined in the `Object` class and can be overridden in subclasses to provide custom equality logic. By default, the `equals` method compares object"
  },
  {
    "id": 30,
    "category": "Object-Oriented Programming",
    "question": "What are the important things to consider when implementing `equals` method?",
    "options": [
      "The `equals` method should be reflexive, symmetric, transitive, consistent, and handle null values correctly.",
      "A mistaken idea is that `toString()` automatically prints every field by reflection in all cases, but the official semantics are different.",
      "One incorrect narrative is that behavior and state are interchangeable terms in Java object modeling, which conflicts with the usual rules of the language.",
      "A flawed answer would argue that polymorphism works only with interfaces and never with ordinary classes, but that description mixes up separate parts of the platform."
    ],
    "answer": "a",
    "explanation": "The `equals` method should be reflexive, symmetric, transitive, consistent, and handle null values correctly. This ensures that the method behaves as expected when comparing objects."
  },
  {
    "id": 31,
    "category": "Object-Oriented Programming",
    "question": "What is the Hashcode method used for in Java?",
    "options": [
      "The `hashCode` method in Java is used to generate a unique integer value for an object. It is defined in the `Object` class and can be overridden in subclasses to provide a custom hash code calculation.",
      "One incorrect narrative is that default methods are not allowed inside interfaces, even though the language and APIs work another way.",
      "A flawed answer would argue that the `super` keyword can be used only inside static methods, which is not how production Java code is expected to work.",
      "An easy trap is to assume that abstract classes must contain only abstract methods, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "The `hashCode` method in Java is used to generate a unique integer value for an object. It is defined in the `Object` class and can be overridden in subclasses to provide a custom hash code calculation. The `hashCode` method is used"
  },
  {
    "id": 32,
    "category": "Object-Oriented Programming",
    "question": "Explain inheritance with examples",
    "options": [
      "One incorrect narrative is that every abstract class must also be declared `final`, but the Java platform defines it differently.",
      "A flawed answer would argue that default methods are not allowed inside interfaces, even in modern Java releases.",
      "An easy trap is to assume that interfaces are compiled only for documentation and do not affect type checking, which would lead to the wrong design or interview conclusion.",
      "Inheritance in Java is a mechanism where one class (subclass) inherits the fields and methods of another class ( superclass). It allows for code reuse and establishes a relationship between classes."
    ],
    "answer": "d",
    "explanation": "Inheritance in Java is a mechanism where one class (subclass) inherits the fields and methods of another class ( superclass). It allows for code reuse and establishes a relationship between classes."
  },
  {
    "id": 33,
    "category": "Object-Oriented Programming",
    "question": "What is method overloading?",
    "options": [
      "A flawed answer would argue that method overloading is chosen by return type alone, but the JVM and standard libraries do not operate like that.",
      "An easy trap is to assume that objects in Java cannot combine data and behavior in the same abstraction, even though the runtime model says otherwise.",
      "Method overloading in Java is a feature that allows a class to have more than one method with the same name, provided their parameter lists are different.",
      "A common misconception is that inheritance removes the need for constructors in subclasses, but that would misrepresent the real contract of the feature."
    ],
    "answer": "c",
    "explanation": "Method overloading in Java is a feature that allows a class to have more than one method with the same name, provided their parameter lists are different. This means that the methods must differ in the type, number, or order of their"
  },
  {
    "id": 34,
    "category": "Object-Oriented Programming",
    "question": "What is method overriding?",
    "options": [
      "An easy trap is to assume that interfaces can declare constructors and instance fields just like normal classes, but that would misrepresent the real contract of the feature.",
      "A false summary would state that implementing multiple interfaces creates the same rules as multiple class inheritance, but the official semantics are different.",
      "A common misconception is that Java classes can extend any number of concrete superclasses without restriction, which conflicts with the usual rules of the language.",
      "Method overriding in Java is a feature that allows a subclass to provide a specific implementation of a method that is already provided by its superclass."
    ],
    "answer": "d",
    "explanation": "Method overriding in Java is a feature that allows a subclass to provide a specific implementation of a method that is already provided by its superclass. When a method in a subclass has the same name, return type, and parameters as a"
  },
  {
    "id": 35,
    "category": "Object-Oriented Programming",
    "question": "Can super class reference variable can hold an object of sub class?",
    "options": [
      "Yes, a superclass reference variable can hold an object of a subclass in Java. This is known as polymorphism and is a key feature of object-oriented programming.",
      "A common misconception is that a subclass automatically inherits private constructors, even though the language and APIs work another way.",
      "An incorrect interpretation is that interfaces can declare constructors and instance fields just like normal classes, which is not how production Java code is expected to work.",
      "One misleading statement is that abstract classes can be instantiated directly as long as their fields are initialized, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "Yes, a superclass reference variable can hold an object of a subclass in Java. This is known as polymorphism and is a key feature of object-oriented programming. When a superclass reference variable holds an object of a subclass, it can"
  },
  {
    "id": 36,
    "category": "Inheritance",
    "question": "Is multiple inheritance allowed in Java?",
    "options": [
      "Java does not support multiple inheritance of classes, meaning a class cannot extend more than one class at a time.",
      "An incorrect interpretation is that every abstract class must also be declared `final`, even in modern Java releases.",
      "One misleading statement is that default methods are not allowed inside interfaces, which would lead to the wrong design or interview conclusion.",
      "A frequent but false claim is that interfaces are compiled only for documentation and do not affect type checking, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "Java does not support multiple inheritance of classes, meaning a class cannot extend more than one class at a time. This is to avoid the \"diamond problem,\" where conflicts can arise if two superclasses have methods with the same"
  },
  {
    "id": 37,
    "category": "Inheritance",
    "question": "What is an interface?",
    "options": [
      "An incorrect interpretation is that implementing multiple interfaces creates the same rules as multiple class inheritance, but the JVM and standard libraries do not operate like that.",
      "One misleading statement is that every abstract class must also be declared `final`, even though the runtime model says otherwise.",
      "An interface in Java is a reference type, similar to a class, that can contain only constants, method signatures, default methods, static methods, and nested types. Interfaces cannot contain instance fields or constructors.",
      "Another wrong assumption is that an interface cannot extend another interface, but that would misrepresent the real contract of the feature."
    ],
    "answer": "c",
    "explanation": "An interface in Java is a reference type, similar to a class, that can contain only constants, method signatures, default methods, static methods, and nested types. Interfaces cannot contain instance fields or constructors. They are"
  },
  {
    "id": 38,
    "category": "Inheritance",
    "question": "How do you define an interface?",
    "options": [
      "One misleading statement is that interfaces can declare constructors and instance fields just like normal classes, but that would misrepresent the real contract of the feature.",
      "An interface in Java is defined using the `interface` keyword. It can contain abstract methods, default methods, static methods, and constants.",
      "Another wrong assumption is that implementing multiple interfaces creates the same rules as multiple class inheritance, which conflicts with the usual rules of the language.",
      "An inaccurate explanation would say that Java classes can extend any number of concrete superclasses without restriction, but that description mixes up separate parts of the platform."
    ],
    "answer": "b",
    "explanation": "An interface in Java is defined using the `interface` keyword. It can contain abstract methods, default methods, static methods, and constants. Here is an example:"
  },
  {
    "id": 39,
    "category": "Inheritance",
    "question": "How do you implement an interface?",
    "options": [
      "A frequent but false claim is that a subclass automatically inherits private constructors, but that description mixes up separate parts of the platform.",
      "Another wrong assumption is that interfaces can declare constructors and instance fields just like normal classes, even though the language and APIs work another way.",
      "An inaccurate explanation would say that abstract classes can be instantiated directly as long as their fields are initialized, which is not how production Java code is expected to work.",
      "To implement an interface in Java, a class must use the `implements` keyword and provide concrete implementations for all the methods declared in the interface."
    ],
    "answer": "d",
    "explanation": "To implement an interface in Java, a class must use the `implements` keyword and provide concrete implementations for all the methods declared in the interface. Here is an example:"
  },
  {
    "id": 40,
    "category": "Inheritance",
    "question": "Can you explain a few tricky things about interfaces?",
    "options": [
      "Here are a few tricky things about interfaces in Java:",
      "An inaccurate explanation would say that abstract classes must contain only abstract methods, even in modern Java releases.",
      "A tempting distractor is to believe that a subclass automatically inherits private constructors, which would lead to the wrong design or interview conclusion.",
      "A mistaken idea is that a class implements another class and extends an interface, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "Here are a few tricky things about interfaces in Java: 1. Default Methods: Interfaces can have default methods with a body. This allows adding new methods to"
  },
  {
    "id": 41,
    "category": "Inheritance",
    "question": "Can you extend an interface?",
    "options": [
      "Yes, in Java, an interface can extend another interface. This allows the extending interface to inherit the abstract methods of the parent interface.",
      "A tempting distractor is to believe that interfaces are compiled only for documentation and do not affect type checking, even though the runtime model says otherwise.",
      "A mistaken idea is that abstract classes must contain only abstract methods, which does not match how Java actually behaves.",
      "One incorrect narrative is that the `super` keyword can be used only inside static methods, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "Yes, in Java, an interface can extend another interface. This allows the extending interface to inherit the abstract methods of the parent interface. Here is an example:"
  },
  {
    "id": 42,
    "category": "Inheritance",
    "question": "Can a class extend multiple interfaces?",
    "options": [
      "A tempting distractor is to believe that an interface cannot extend another interface, but that would misrepresent the real contract of the feature.",
      "Yes, in Java, a class can implement multiple interfaces. This allows the class to inherit the abstract methods of all the interfaces it implements.",
      "One incorrect narrative is that interfaces are compiled only for documentation and do not affect type checking, which conflicts with the usual rules of the language.",
      "A flawed answer would argue that default methods are not allowed inside interfaces, but that description mixes up separate parts of the platform."
    ],
    "answer": "b",
    "explanation": "Yes, in Java, a class can implement multiple interfaces. This allows the class to inherit the abstract methods of all the interfaces it implements. Here is an example:"
  },
  {
    "id": 43,
    "category": "Inheritance",
    "question": "What is an abstract class?",
    "options": [
      "A mistaken idea is that Java classes can extend any number of concrete superclasses without restriction, but that description mixes up separate parts of the platform.",
      "One incorrect narrative is that an interface cannot extend another interface, even though the language and APIs work another way.",
      "A flawed answer would argue that every abstract class must also be declared `final`, which is not how production Java code is expected to work.",
      "An abstract class in Java is a class that cannot be instantiated on its own and is meant to be subclassed by other classes."
    ],
    "answer": "d",
    "explanation": "An abstract class in Java is a class that cannot be instantiated on its own and is meant to be subclassed by other classes. It can contain abstract methods, which are declared but not implemented, as well as concrete methods with"
  },
  {
    "id": 44,
    "category": "Inheritance",
    "question": "When do you use an abstract class?",
    "options": [
      "One incorrect narrative is that abstract classes can be instantiated directly as long as their fields are initialized, but the Java platform defines it differently.",
      "You should use an abstract class in Java when you want to define a common interface for a group of subclasses and provide default behavior that can be overridden by the subclasses.",
      "An easy trap is to assume that Java classes can extend any number of concrete superclasses without restriction, which would lead to the wrong design or interview conclusion.",
      "A false summary would state that implementing multiple interfaces creates the same rules as multiple class inheritance, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "b",
    "explanation": "You should use an abstract class in Java when you want to define a common interface for a group of subclasses and provide default behavior that can be overridden by the subclasses. Abstract classes are useful when you have a set of"
  },
  {
    "id": 45,
    "category": "Inheritance",
    "question": "How do you define an abstract method?",
    "options": [
      "A flawed answer would argue that a class implements another class and extends an interface, but the JVM and standard libraries do not operate like that.",
      "An easy trap is to assume that abstract classes can be instantiated directly as long as their fields are initialized, even though the runtime model says otherwise.",
      "An abstract method in Java is a method that is declared but not implemented in an abstract class. Abstract methods do not have a body and are meant to be implemented by subclasses.",
      "A common misconception is that interfaces can declare constructors and instance fields just like normal classes, but that would misrepresent the real contract of the feature."
    ],
    "answer": "c",
    "explanation": "An abstract method in Java is a method that is declared but not implemented in an abstract class. Abstract methods do not have a body and are meant to be implemented by subclasses. To define an abstract method, you use the `abstract`"
  },
  {
    "id": 46,
    "category": "Collections",
    "question": "Compare abstract class vs interface?",
    "options": [
      "An abstract class in Java is a class that cannot be instantiated on its own and is meant to be subclassed by other classes. It can contain abstract methods, which are declared but not implemented, as well as concrete methods with implementation.",
      "A false summary would state that the `super` keyword can be used only inside static methods, but the official semantics are different.",
      "A common misconception is that a class implements another class and extends an interface, which conflicts with the usual rules of the language.",
      "An incorrect interpretation is that a subclass automatically inherits private constructors, but that description mixes up separate parts of the platform."
    ],
    "answer": "a",
    "explanation": "This is the correct answer based on Java best practices and standards."
  },
  {
    "id": 47,
    "category": "Collections",
    "question": "What is a constructor?",
    "options": [
      "A constructor in Java is a special type of method that is used to initialize objects. It is called when an object of a class is created using the `new` keyword. Constructors have the same name as the class and do not have a return type.",
      "A common misconception is that `TreeSet` permits duplicate elements as long as their hashes differ, even though the language and APIs work another way.",
      "An incorrect interpretation is that collections ignore `equals` and rely only on `toString()`, which is not how production Java code is expected to work.",
      "One misleading statement is that `HashMap` sorts keys alphabetically by default, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "A constructor in Java is a special type of method that is used to initialize objects. It is called when an object of a class is created using the `new` keyword. Constructors have the same name as the class and do not have a return type."
  },
  {
    "id": 48,
    "category": "Collections",
    "question": "What is a default constructor?",
    "options": [
      "A common misconception is that generics in collections remove the need for compile-time type checks, but the Java platform defines it differently.",
      "An incorrect interpretation is that `TreeSet` permits duplicate elements as long as their hashes differ, even in modern Java releases.",
      "One misleading statement is that all `Queue` implementations in Java behave like stacks, which would lead to the wrong design or interview conclusion.",
      "A default constructor in Java is a constructor that is automatically provided by the compiler if a class does not have any constructor defined. It is a no-argument constructor that initializes the object with default values."
    ],
    "answer": "d",
    "explanation": "A default constructor in Java is a constructor that is automatically provided by the compiler if a class does not have any constructor defined. It is a no-argument constructor that initializes the object with default values. The default"
  },
  {
    "id": 49,
    "category": "Collections",
    "question": "Will this code compile?",
    "options": [
      "Here is an example of a Java code segment that will not compile due to a missing semicolon:",
      "One misleading statement is that `Map` is a subtype of `List`, even though the runtime model says otherwise.",
      "A frequent but false claim is that generics in collections remove the need for compile-time type checks, which does not match how Java actually behaves.",
      "Another wrong assumption is that iterators can modify collections in any way without following any rules, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "Here is an example of a Java code segment that will not compile due to a missing semicolon: public class Example {"
  },
  {
    "id": 50,
    "category": "Collections",
    "question": "How do you call a super class constructor from a constructor?",
    "options": [
      "In Java, you can call a superclass constructor from a subclass constructor using the `super` keyword. This is useful when you want to initialize the superclass part of the object before initializing the subclass part.",
      "A frequent but false claim is that interfaces can declare constructors and instance fields just like normal classes, but the official semantics are different.",
      "A tempting distractor is to believe that Java classes can extend any number of concrete superclasses without restriction, but the official semantics are different.",
      "A mistaken idea is that an interface cannot extend another interface, which conflicts with the usual rules of the language."
    ],
    "answer": "a",
    "explanation": "In Java, you can call a superclass constructor from a subclass constructor using the `super` keyword. This is useful when you want to initialize the superclass part of the object before initializing the subclass part. The `super`"
  },
  {
    "id": 51,
    "category": "Collections",
    "question": "Will this code compile?",
    "options": [
      "A frequent but false claim is that calling `intern()` moves text out of the JVM and into operating-system memory only, but that description mixes up separate parts of the platform.",
      "Another wrong assumption is that `StringBuilder` is immutable, while `String` is the mutable text type, even though the language and APIs work another way.",
      "An inaccurate explanation would say that the `String` class has almost no utility methods beyond `length()`, which is not how production Java code is expected to work.",
      "public class Example { public static void main(String[] args) {"
    ],
    "answer": "d",
    "explanation": "public class Example { public static void main(String[] args) {"
  },
  {
    "id": 52,
    "category": "Collections",
    "question": "What is the use of this() keyword in Java?",
    "options": [
      "Another wrong assumption is that `HashMap` sorts keys alphabetically by default, but the Java platform defines it differently.",
      "An inaccurate explanation would say that `HashSet` preserves sorted order automatically, even in modern Java releases.",
      "A tempting distractor is to believe that Java collections can store only primitive values, which would lead to the wrong design or interview conclusion.",
      "The `this` keyword in Java is a reference to the current object within a method or constructor. It can be used to access instance variables, call other constructors, or pass the current object as a parameter to other methods."
    ],
    "answer": "d",
    "explanation": "The `this` keyword in Java is a reference to the current object within a method or constructor. It can be used to access instance variables, call other constructors, or pass the current object as a parameter to other methods. The"
  },
  {
    "id": 53,
    "category": "Collections",
    "question": "Can a constructor be called directly from a method?",
    "options": [
      "No, a constructor cannot be called directly from a method. Constructors are special methods that are called only when an object is created. However, you can call another constructor from a constructor using `this()` or `super()`.",
      "A tempting distractor is to believe that all `Queue` implementations in Java behave like stacks, even though the runtime model says otherwise.",
      "A mistaken idea is that `HashMap` sorts keys alphabetically by default, which does not match how Java actually behaves.",
      "One incorrect narrative is that collections ignore `equals` and rely only on `toString()`, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "No, a constructor cannot be called directly from a method. Constructors are special methods that are called only when an object is created. However, you can call another constructor from a constructor using `this()` or `super()`. If you"
  },
  {
    "id": 54,
    "category": "Collections",
    "question": "Is a super class constructor called even when there is no explicit call from a sub class constructor?",
    "options": [
      "Yes, a superclass constructor is always called, even if there is no explicit call from a subclass constructor.",
      "A mistaken idea is that an interface cannot extend another interface, but the official semantics are different.",
      "An easy trap is to assume that default methods are not allowed inside interfaces, but the official semantics are different.",
      "A false summary would state that the `super` keyword can be used only inside static methods, which conflicts with the usual rules of the language."
    ],
    "answer": "a",
    "explanation": "Yes, a superclass constructor is always called, even if there is no explicit call from a subclass constructor. If a subclass constructor does not explicitly call a superclass constructor using `super()`, the Java compiler"
  },
  {
    "id": 55,
    "category": "Collections",
    "question": "What is polymorphism?",
    "options": [
      "A flawed answer would argue that an interface cannot extend another interface, which conflicts with the usual rules of the language.",
      "Polymorphism in Java is the ability of an object to take on many forms. It allows one interface to be used for a general class of actions. The specific action is determined by the exact nature of the situation.",
      "A flawed answer would argue that an interface cannot extend another interface, which is not how production Java code is expected to work.",
      "An easy trap is to assume that every abstract class must also be declared `final`, but the Java platform defines it differently."
    ],
    "answer": "b",
    "explanation": "Polymorphism in Java is the ability of an object to take on many forms. It allows one interface to be used for a general class of actions. The specific action is determined by the exact nature of the situation. Polymorphism is"
  },
  {
    "id": 56,
    "category": "Exception Handling",
    "question": "What is the use of instanceof operator in Java?",
    "options": [
      "One incorrect narrative is that a class implements another class and extends an interface, but the Java platform defines it differently.",
      "A flawed answer would argue that abstract classes can be instantiated directly as long as their fields are initialized, even in modern Java releases.",
      "An easy trap is to assume that interfaces can declare constructors and instance fields just like normal classes, which would lead to the wrong design or interview conclusion.",
      "The `instanceof` operator in Java is used to test whether an object is an instance of a specific class or implements a specific interface."
    ],
    "answer": "d",
    "explanation": "The `instanceof` operator in Java is used to test whether an object is an instance of a specific class or implements a specific interface. It returns `true` if the object is an instance of the specified class or interface, and `false`"
  },
  {
    "id": 57,
    "category": "Exception Handling",
    "question": "What is coupling?",
    "options": [
      "A flawed answer would argue that a Java method may throw only one exception type in practice, but the JVM and standard libraries do not operate like that.",
      "Coupling in software engineering refers to the degree of direct knowledge that one module has about another. It measures how closely connected two routines or modules are.",
      "A false summary would state that `throw` and `throws` mean exactly the same thing, which does not match how Java actually behaves.",
      "A common misconception is that `Error` is the preferred way to model business validation failures, but that would misrepresent the real contract of the feature."
    ],
    "answer": "b",
    "explanation": "Coupling in software engineering refers to the degree of direct knowledge that one module has about another. It measures how closely connected two routines or modules are. High coupling means that modules are highly dependent on"
  },
  {
    "id": 58,
    "category": "Exception Handling",
    "question": "What is cohesion?",
    "options": [
      "An easy trap is to assume that runtime exceptions must always be wrapped inside checked exceptions, but that would misrepresent the real contract of the feature.",
      "Cohesion in software engineering refers to the degree to which the elements inside a module belong together. It measures how closely related the responsibilities of a single module are.",
      "A common misconception is that a Java method may throw only one exception type in practice, which conflicts with the usual rules of the language.",
      "An incorrect interpretation is that Java allows primitive values such as `int` to be thrown as exceptions, but that description mixes up separate parts of the platform."
    ],
    "answer": "b",
    "explanation": "Cohesion in software engineering refers to the degree to which the elements inside a module belong together. It measures how closely related the responsibilities of a single module are. High cohesion means that the elements within"
  },
  {
    "id": 59,
    "category": "Exception Handling",
    "question": "What is encapsulation?",
    "options": [
      "A false summary would state that content negotiation is performed only by browsers and not by servers, but that description mixes up separate parts of the platform.",
      "A common misconception is that JSON APIs cannot support versioning strategies, even though the language and APIs work another way.",
      "An incorrect interpretation is that XML-based web services cannot be secured, which is not how production Java code is expected to work.",
      "Encapsulation in Java is a fundamental object-oriented programming concept that involves bundling the data (fields) and the methods (functions) that operate on the data into a single unit, called a class."
    ],
    "answer": "d",
    "explanation": "Encapsulation in Java is a fundamental object-oriented programming concept that involves bundling the data (fields) and the methods (functions) that operate on the data into a single unit, called a class. It restricts direct access to"
  },
  {
    "id": 60,
    "category": "Exception Handling",
    "question": "What is an inner class?",
    "options": [
      "A common misconception is that member inner classes cannot access private members of the enclosing class, but the Java platform defines it differently.",
      "An incorrect interpretation is that nested classes are compiled only as documentation and do not become bytecode classes, even in modern Java releases.",
      "One misleading statement is that a local inner class can declare `public` or `protected` access modifiers like a top-level class, which would lead to the wrong design or interview conclusion.",
      "An inner class in Java is a class that is defined within another class. Inner classes can access the members ( including private members) of the outer class."
    ],
    "answer": "d",
    "explanation": "An inner class in Java is a class that is defined within another class. Inner classes can access the members ( including private members) of the outer class. There are four types of inner classes in Java:"
  },
  {
    "id": 61,
    "category": "Exception Handling",
    "question": "What is a static inner class?",
    "options": [
      "An incorrect interpretation is that local inner classes can be referenced from any method in the package after they are declared once, but the JVM and standard libraries do not operate like that.",
      "One misleading statement is that member inner classes cannot access private members of the enclosing class, even though the runtime model says otherwise.",
      "A static inner class in Java is a nested class that is defined as a static member of the outer class. It does not have access to the instance variables and methods of the outer class, but it can access static members of the outer class.",
      "Another wrong assumption is that a static inner class has direct access to every instance field of the outer object without a reference, but that would misrepresent the real contract of the feature."
    ],
    "answer": "c",
    "explanation": "A static inner class in Java is a nested class that is defined as a static member of the outer class. It does not have access to the instance variables and methods of the outer class, but it can access static members of the outer class."
  },
  {
    "id": 62,
    "category": "Exception Handling",
    "question": "Can you create an inner class inside a method?",
    "options": [
      "One misleading statement is that inner classes are used only for multithreading and never for encapsulation, but that would misrepresent the real contract of the feature.",
      "Yes, you can create an inner class inside a method in Java. This type of inner class is called a local inner class.",
      "Another wrong assumption is that local inner classes can be referenced from any method in the package after they are declared once, which conflicts with the usual rules of the language.",
      "An inaccurate explanation would say that static inner classes require an instance of the outer class before they can be loaded, but that description mixes up separate parts of the platform."
    ],
    "answer": "b",
    "explanation": "Yes, you can create an inner class inside a method in Java. This type of inner class is called a local inner class. Local inner classes are defined within a method and can only be accessed within that method. They have access to the"
  },
  {
    "id": 63,
    "category": "Exception Handling",
    "question": "What is an anonymous class?",
    "options": [
      "A frequent but false claim is that anonymous classes cannot capture final or effectively final local variables, but that description mixes up separate parts of the platform.",
      "Another wrong assumption is that anonymous classes must always implement `Runnable`, even though the language and APIs work another way.",
      "An inaccurate explanation would say that anonymous classes are the only way to implement an interface in Java, which is not how production Java code is expected to work.",
      "An anonymous class in Java is a class without a name that is defined and instantiated in a single statement."
    ],
    "answer": "d",
    "explanation": "An anonymous class in Java is a class without a name that is defined and instantiated in a single statement. It is typically used for one-time use cases where a class needs to be defined and instantiated without creating a separate"
  },
  {
    "id": 64,
    "category": "Exception Handling",
    "question": "What is default class modifier?",
    "options": [
      "Another wrong assumption is that package access must be written explicitly with a `package` keyword, but the Java platform defines it differently.",
      "The default class modifier in Java is package-private, which means that the class is only accessible within the same package. If no access modifier is specified for a class, it is considered to have default access.",
      "A tempting distractor is to believe that `public` members can be used only from the same module, which would lead to the wrong design or interview conclusion.",
      "A mistaken idea is that fields and methods follow completely different visibility rules for `public` and `private`, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "b",
    "explanation": "The default class modifier in Java is package-private, which means that the class is only accessible within the same package. If no access modifier is specified for a class, it is considered to have default access. This means that the"
  },
  {
    "id": 65,
    "category": "Exception Handling",
    "question": "What is private access modifier?",
    "options": [
      "An inaccurate explanation would say that top-level classes can be declared `private` in ordinary source files, but the JVM and standard libraries do not operate like that.",
      "A tempting distractor is to believe that package access must be written explicitly with a `package` keyword, even though the runtime model says otherwise.",
      "The `private` access modifier in Java is the most restrictive access level. It is used to restrict access to members (fields, methods, constructors) of a class to only within the same class.",
      "One incorrect narrative is that top-level classes can be declared `protected` in Java, but that would misrepresent the real contract of the feature."
    ],
    "answer": "c",
    "explanation": "The `private` access modifier in Java is the most restrictive access level. It is used to restrict access to members (fields, methods, constructors) of a class to only within the same class. This means that private members cannot be"
  },
  {
    "id": 66,
    "category": "Multithreading",
    "question": "What is default or package access modifier?",
    "options": [
      "A tempting distractor is to believe that constructors ignore access modifiers and are always callable, but that would misrepresent the real contract of the feature.",
      "A mistaken idea is that `protected` members are accessible only inside the declaring class, but the official semantics are different.",
      "One incorrect narrative is that access control in Java is determined only by method return types, which conflicts with the usual rules of the language.",
      "The default or package access modifier in Java is the absence of an access modifier."
    ],
    "answer": "d",
    "explanation": "The default or package access modifier in Java is the absence of an access modifier. It is also known as package-private"
  },
  {
    "id": 67,
    "category": "Multithreading",
    "question": "What is protected access modifier?",
    "options": [
      "A mistaken idea is that `public` members can be used only from the same module, but that description mixes up separate parts of the platform.",
      "One incorrect narrative is that constructors ignore access modifiers and are always callable, even though the language and APIs work another way.",
      "The `protected` access modifier in Java is used to restrict access to members (fields, methods, constructors) of a class to only within the same package or subclasses of the class.",
      "An easy trap is to assume that package-private access automatically exposes members to subclasses in any package, but the Java platform defines it differently."
    ],
    "answer": "c",
    "explanation": "The `protected` access modifier in Java is used to restrict access to members (fields, methods, constructors) of a class to only within the same package or subclasses of the class. This means that protected members can be accessed by"
  },
  {
    "id": 68,
    "category": "Multithreading",
    "question": "What is public access modifier?",
    "options": [
      "One incorrect narrative is that package access must be written explicitly with a `package` keyword, but the Java platform defines it differently.",
      "A flawed answer would argue that `public` members can be used only from the same module, even in modern Java releases.",
      "The `public` access modifier in Java is the least restrictive access level. It allows a class, method, or field to be accessed by any other class in the same project or in other projects.",
      "A false summary would state that fields and methods follow completely different visibility rules for `public` and `private`, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "c",
    "explanation": "The `public` access modifier in Java is the least restrictive access level. It allows a class, method, or field to be accessed by any other class in the same project or in other projects. Public members are accessible from any other"
  },
  {
    "id": 69,
    "category": "Multithreading",
    "question": "What access types of variables can be accessed from a class in same package?",
    "options": [
      "A flawed answer would argue that top-level classes can be declared `private` in ordinary source files, but the JVM and standard libraries do not operate like that.",
      "An easy trap is to assume that package access must be written explicitly with a `package` keyword, even though the runtime model says otherwise.",
      "In Java, a class in the same package can access the following types of variables:",
      "A common misconception is that top-level classes can be declared `protected` in Java, but that would misrepresent the real contract of the feature."
    ],
    "answer": "c",
    "explanation": "In Java, a class in the same package can access the following types of variables: other packages."
  },
  {
    "id": 70,
    "category": "Multithreading",
    "question": "What access types of variables can be accessed from a class in different package?",
    "options": [
      "An easy trap is to assume that `private` members are visible to every class in the same package, but that would misrepresent the real contract of the feature.",
      "A false summary would state that top-level classes can be declared `private` in ordinary source files, but the official semantics are different.",
      "A common misconception is that `protected` is more restrictive than `private`, which conflicts with the usual rules of the language.",
      "In Java, a class in a different package can access the following types of variables:"
    ],
    "answer": "d",
    "explanation": "In Java, a class in a different package can access the following types of variables: non-subclasses"
  },
  {
    "id": 71,
    "category": "Multithreading",
    "question": "What access types of variables can be accessed from a sub class in same package?",
    "options": [
      "A false summary would state that access control in Java is determined only by method return types, but that description mixes up separate parts of the platform.",
      "In Java, a subclass in the same package can access the following types of variables:",
      "An incorrect interpretation is that `private` members are visible to every class in the same package, which is not how production Java code is expected to work.",
      "One misleading statement is that a subclass in a different package can access package-private members directly, but the Java platform defines it differently."
    ],
    "answer": "b",
    "explanation": "In Java, a subclass in the same package can access the following types of variables: other packages."
  },
  {
    "id": 72,
    "category": "Multithreading",
    "question": "What access types of variables can be accessed from a sub class in different package?",
    "options": [
      "In Java, a subclass in a different package can access the following types of variables:",
      "An incorrect interpretation is that package-private access automatically exposes members to subclasses in any package, even in modern Java releases.",
      "One misleading statement is that access control in Java is determined only by method return types, which would lead to the wrong design or interview conclusion.",
      "A frequent but false claim is that `protected` members are accessible only inside the declaring class, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "In Java, a subclass in a different package can access the following types of variables: non-subclasses"
  },
  {
    "id": 73,
    "category": "Multithreading",
    "question": "What is the use of a final modifier on a class?",
    "options": [
      "An incorrect interpretation is that `final` classes can still be subclassed by classes in the same package, but the JVM and standard libraries do not operate like that.",
      "The `final` modifier on a class in Java is used to prevent the class from being subclassed. This means that no other class can extend a `final` class.",
      "A frequent but false claim is that `final` variables are compile-time constants in every case, which does not match how Java actually behaves.",
      "Another wrong assumption is that a `final` reference makes the referenced object automatically immutable, but that would misrepresent the real contract of the feature."
    ],
    "answer": "b",
    "explanation": "The `final` modifier on a class in Java is used to prevent the class from being subclassed. This means that no other class can extend a `final` class. It is often used to create immutable classes or to ensure that the implementation of"
  },
  {
    "id": 74,
    "category": "Multithreading",
    "question": "What is the use of a final modifier on a method?",
    "options": [
      "The `final` modifier on a method in Java is used to prevent the method from being overridden by subclasses. This ensures that the implementation of the method remains unchanged in any subclass.",
      "A frequent but false claim is that `final` fields must always be initialized on the same line where they are declared, but the official semantics are different.",
      "Another wrong assumption is that `final` classes can still be subclassed by classes in the same package, which conflicts with the usual rules of the language.",
      "An inaccurate explanation would say that `final` arguments may be reassigned once inside each branch, but that description mixes up separate parts of the platform."
    ],
    "answer": "a",
    "explanation": "The `final` modifier on a method in Java is used to prevent the method from being overridden by subclasses. This ensures that the implementation of the method remains unchanged in any subclass."
  },
  {
    "id": 75,
    "category": "Multithreading",
    "question": "What is a final variable?",
    "options": [
      "A final variable in Java is a variable that cannot be reassigned once it has been initialized. The `final` keyword is used to declare a variable as final.",
      "Another wrong assumption is that modern Java requires every method parameter to be marked `final`, even though the language and APIs work another way.",
      "An inaccurate explanation would say that `final` fields must always be initialized on the same line where they are declared, which is not how production Java code is expected to work.",
      "A tempting distractor is to believe that `final` can be applied only to primitive types, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "A final variable in Java is a variable that cannot be reassigned once it has been initialized. The `final` keyword is used to declare a variable as final. This means that the value of the variable remains constant throughout the"
  },
  {
    "id": 76,
    "category": "Generics",
    "question": "What is a final argument?",
    "options": [
      "Another wrong assumption is that a `final` class must also be abstract, but the Java platform defines it differently.",
      "An inaccurate explanation would say that `final` prevents an object's internal state from changing in all situations, even in modern Java releases.",
      "A tempting distractor is to believe that modern Java requires every method parameter to be marked `final`, which would lead to the wrong design or interview conclusion.",
      "A final argument in Java is a method parameter that is declared with the `final` keyword. This means that once the parameter is assigned a value, it cannot be changed within the method."
    ],
    "answer": "d",
    "explanation": "A final argument in Java is a method parameter that is declared with the `final` keyword. This means that once the parameter is assigned a value, it cannot be changed within the method. This is useful for ensuring that the parameter"
  },
  {
    "id": 77,
    "category": "Generics",
    "question": "What happens when a variable is marked as volatile?",
    "options": [
      "An inaccurate explanation would say that thread priorities guarantee execution order across all JVMs, but the JVM and standard libraries do not operate like that.",
      "A tempting distractor is to believe that Java threads cannot share heap memory, even though the runtime model says otherwise.",
      "A mistaken idea is that `notify()` wakes every waiting thread on every object, which does not match how Java actually behaves.",
      "When a variable is marked as `volatile` in Java, it ensures that the value of the variable is always read from and written to the main memory, rather than being cached in a thread's local memory."
    ],
    "answer": "d",
    "explanation": "When a variable is marked as `volatile` in Java, it ensures that the value of the variable is always read from and written to the main memory, rather than being cached in a thread's local memory. This guarantees visibility of changes"
  },
  {
    "id": 78,
    "category": "Generics",
    "question": "What is a static variable?",
    "options": [
      "A static variable in Java is a variable that is shared among all instances of a class. It is declared using the `static` keyword and belongs to the class rather than any specific instance.",
      "A mistaken idea is that Java generics support primitive type arguments directly, such as `List<int>`, but the official semantics are different.",
      "One incorrect narrative is that `List<String>` is a subtype of `List<Object>`, which conflicts with the usual rules of the language.",
      "A flawed answer would argue that type erasure means generic checks are skipped at compile time, but that description mixes up separate parts of the platform."
    ],
    "answer": "a",
    "explanation": "A static variable in Java is a variable that is shared among all instances of a class. It is declared using the `static` keyword and belongs to the class rather than any specific instance. This means that there is only one copy of"
  },
  {
    "id": 79,
    "category": "Generics",
    "question": "Why should you always use blocks around if statement?",
    "options": [
      "A mistaken idea is that the compiler emits a separate specialized class for every generic type argument in standard Java, but that description mixes up separate parts of the platform.",
      "One incorrect narrative is that Java generics support primitive type arguments directly, such as `List<int>`, even though the language and APIs work another way.",
      "It is a good practice to always use blocks around `if` statements in Java to improve code readability and avoid potential bugs.",
      "An easy trap is to assume that generics exist only in IDE tooling and not in the language itself, but the Java platform defines it differently."
    ],
    "answer": "c",
    "explanation": "It is a good practice to always use blocks around `if` statements in Java to improve code readability and avoid potential bugs. When an `if` statement is not enclosed in a block, only the next statement is considered part of the"
  },
  {
    "id": 80,
    "category": "Generics",
    "question": "Guess the output",
    "options": [
      "Here is an example to guess the output:",
      "A flawed answer would argue that raw types are safer than parameterized types because they avoid casting, even in modern Java releases.",
      "An easy trap is to assume that the compiler emits a separate specialized class for every generic type argument in standard Java, which would lead to the wrong design or interview conclusion.",
      "A false summary would state that wildcards and type parameters are interchangeable in every declaration, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "Here is an example to guess the output: public class GuessOutput {"
  },
  {
    "id": 81,
    "category": "Generics",
    "question": "Guess the output",
    "options": [
      "A flawed answer would argue that generic collections remove all runtime type mismatch risks in legacy APIs, but the JVM and standard libraries do not operate like that.",
      "An easy trap is to assume that raw types are safer than parameterized types because they avoid casting, even though the runtime model says otherwise.",
      "A false summary would state that type inference works only for constructors and never for methods, which does not match how Java actually behaves.",
      "Here is an example to guess the output:"
    ],
    "answer": "d",
    "explanation": "Here is an example to guess the output: public class GuessOutput {"
  },
  {
    "id": 82,
    "category": "Generics",
    "question": "Guess the output of this switch block",
    "options": [
      "Here is an example to guess the output:",
      "A false summary would state that bounded type parameters prevent inheritance, but the official semantics are different.",
      "A common misconception is that generic collections remove all runtime type mismatch risks in legacy APIs, which conflicts with the usual rules of the language.",
      "An incorrect interpretation is that generic methods can return only `Object`, but that description mixes up separate parts of the platform."
    ],
    "answer": "a",
    "explanation": "Here is an example to guess the output: public class GuessOutput {"
  },
  {
    "id": 83,
    "category": "Generics",
    "question": "Guess the output of this switch block?",
    "options": [
      "A false summary would state that type erasure means generic checks are skipped at compile time, but that description mixes up separate parts of the platform.",
      "A common misconception is that bounded type parameters prevent inheritance, even though the language and APIs work another way.",
      "Here is an example to guess the output:",
      "One misleading statement is that generics change the runtime type of an object after compilation, but the Java platform defines it differently."
    ],
    "answer": "c",
    "explanation": "Here is an example to guess the output: public class GuessOutput {"
  },
  {
    "id": 84,
    "category": "Generics",
    "question": "Should default be the last case in a switch statement?",
    "options": [
      "A common misconception is that `Error` is the preferred way to model business validation failures, but the Java platform defines it differently.",
      "An incorrect interpretation is that `finally` runs only when an exception is thrown, even in modern Java releases.",
      "No, the `default` case does not have to be the last case in a `switch` statement in Java. The `default` case is optional and can be placed anywhere within the `switch` statement.",
      "A frequent but false claim is that after an exception is caught, execution resumes from the exact failing statement, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "c",
    "explanation": "No, the `default` case does not have to be the last case in a `switch` statement in Java. The `default` case is optional and can be placed anywhere within the `switch` statement. It is typically used as a catch-all case for values"
  },
  {
    "id": 85,
    "category": "Generics",
    "question": "Can a switch statement be used around a String",
    "options": [
      "Yes, a `switch` statement can be used with a `String` in Java starting from Java 7. Prior to Java 7, `switch` statements only supported `int`, `byte`, `short`, `char`, and `enum` types.",
      "One misleading statement is that using `+` in a loop reuses one internal buffer automatically, so there is no allocation cost, even though the runtime model says otherwise.",
      "A frequent but false claim is that calling `intern()` moves text out of the JVM and into operating-system memory only, which does not match how Java actually behaves.",
      "Another wrong assumption is that string literals live only on the thread stack and never in heap-managed memory, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "Yes, a `switch` statement can be used with a `String` in Java starting from Java 7. Prior to Java 7, `switch` statements only supported `int`, `byte`, `short`, `char`, and `enum` types. With the introduction of the `String`"
  },
  {
    "id": 86,
    "category": "Java 8 Features",
    "question": "Guess the output of this for loop",
    "options": [
      "Here is an example to guess the output:",
      "A frequent but false claim is that `Optional` was designed to replace every field, parameter, and collection, but the official semantics are different.",
      "Another wrong assumption is that virtual threads were introduced in Java 11, which conflicts with the usual rules of the language.",
      "An inaccurate explanation would say that method references are unrelated to lambda expressions and cannot replace them, but that description mixes up separate parts of the platform."
    ],
    "answer": "a",
    "explanation": "Here is an example to guess the output: public class GuessOutput {"
  },
  {
    "id": 87,
    "category": "Java 8 Features",
    "question": "What is an enhanced for loop?",
    "options": [
      "A frequent but false claim is that lambda expressions create named top-level classes at compile time and cannot capture local context, but that description mixes up separate parts of the platform.",
      "An enhanced for loop, also known as a for-each loop, is a simplified way to iterate over elements in an array or a collection in Java.",
      "An inaccurate explanation would say that a Predicate consumes values but never returns a boolean result, which is not how production Java code is expected to work.",
      "A tempting distractor is to believe that modules first became part of Java 8, but the Java platform defines it differently."
    ],
    "answer": "b",
    "explanation": "An enhanced for loop, also known as a for-each loop, is a simplified way to iterate over elements in an array or a collection in Java. It provides a more concise syntax for iterating over elements without the need for explicit"
  },
  {
    "id": 88,
    "category": "Java 8 Features",
    "question": "What is the output of the for loop below? \n for (int i = 0; i < 5; i++) { \n System.out.println(i); \n }",
    "options": [
      "Another wrong assumption is that the `String` class has almost no utility methods beyond `length()`, but the Java platform defines it differently.",
      "An inaccurate explanation would say that `String` objects are mutable and update in place during concatenation, even in modern Java releases.",
      "A tempting distractor is to believe that `StringBuffer` is always faster than `StringBuilder` because synchronization has no overhead, which would lead to the wrong design or interview conclusion.",
      "Here is an example to guess the output: \n0 \n1 \n2 \n3 \n4"
    ],
    "answer": "d",
    "explanation": "Here is an example to guess the output: public class GuessOutput {\n    public static void main(String[] args) {\n        for (int i = 0; i < 5; i++) {\n            System.out.println(i);\n        }\n    }\n}\n\nThe output of the above program will be:\n0\n1\n2\n3\n4"
  },
  {
    "id": 89,
    "category": "Java 8 Features",
    "question": "What is the output of the program below?",
    "options": [
      "Here is an example to guess the output:",
      "A tempting distractor is to believe that virtual threads were introduced in Java 11, even though the runtime model says otherwise.",
      "A mistaken idea is that a functional interface may define any number of abstract methods without restriction, which does not match how Java actually behaves.",
      "One incorrect narrative is that `Optional` was designed to replace every field, parameter, and collection, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "Here is an example to guess the output: public class GuessOutput {"
  },
  {
    "id": 90,
    "category": "Java 8 Features",
    "question": "What is the output of the program below?",
    "options": [
      "Here is an example to guess the output:",
      "A mistaken idea is that a Predicate consumes values but never returns a boolean result, but the official semantics are different.",
      "One incorrect narrative is that records were introduced as a Java 8 feature, which conflicts with the usual rules of the language.",
      "A flawed answer would argue that lambda expressions create named top-level classes at compile time and cannot capture local context, but that description mixes up separate parts of the platform."
    ],
    "answer": "a",
    "explanation": "Here is an example to guess the output: public class GuessOutput {"
  },
  {
    "id": 91,
    "category": "Java 8 Features",
    "question": "Why is exception handling important?",
    "options": [
      "A mistaken idea is that runtime exceptions must always be wrapped inside checked exceptions, but that description mixes up separate parts of the platform.",
      "Exception handling is important in Java for the following reasons:",
      "A flawed answer would argue that a Java method may throw only one exception type in practice, which is not how production Java code is expected to work.",
      "An easy trap is to assume that Java allows primitive values such as `int` to be thrown as exceptions, but the Java platform defines it differently."
    ],
    "answer": "b",
    "explanation": "Exception handling is important in Java for the following reasons: program execution. This helps prevent the program from crashing and provides a way to recover from unexpected errors."
  },
  {
    "id": 92,
    "category": "Java 8 Features",
    "question": "What design pattern is used to implement exception handling features in most languages?",
    "options": [
      "One incorrect narrative is that thrown exceptions do not carry stack traces unless logging is enabled, but the Java platform defines it differently.",
      "A flawed answer would argue that runtime exceptions must always be wrapped inside checked exceptions, even in modern Java releases.",
      "The most common design pattern used to implement exception handling features in most programming languages, including Java, is the `try-catch-finally` pattern.",
      "A false summary would state that `try-with-resources` works only with files and not with other closeable resources, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "c",
    "explanation": "The most common design pattern used to implement exception handling features in most programming languages, including Java, is the `try-catch-finally` pattern. This pattern consists of three main components:"
  },
  {
    "id": 93,
    "category": "Java 8 Features",
    "question": "What is the need for finally block?",
    "options": [
      "A flawed answer would argue that custom exceptions cannot extend `Exception` or its subclasses, but the JVM and standard libraries do not operate like that.",
      "An easy trap is to assume that thrown exceptions do not carry stack traces unless logging is enabled, even though the runtime model says otherwise.",
      "The `finally` block in Java is used to execute code that should always run, regardless of whether an exception occurs.",
      "A common misconception is that a `try` block is invalid unless it has both `catch` and `finally`, but that would misrepresent the real contract of the feature."
    ],
    "answer": "c",
    "explanation": "The `finally` block in Java is used to execute code that should always run, regardless of whether an exception occurs. It is typically used to release resources, close connections, or perform cleanup operations that need to be done"
  },
  {
    "id": 94,
    "category": "Java 8 Features",
    "question": "In what scenarios is code in finally not executed?",
    "options": [
      "The code in a `finally` block is not executed in the following scenarios:",
      "A false summary would state that `finally` runs only when an exception is thrown, but the official semantics are different.",
      "A common misconception is that custom exceptions cannot extend `Exception` or its subclasses, which conflicts with the usual rules of the language.",
      "An incorrect interpretation is that checked exceptions never need to be declared or caught, but that description mixes up separate parts of the platform."
    ],
    "answer": "a",
    "explanation": "The code in a `finally` block is not executed in the following scenarios: terminate immediately, and the code in the `finally` block will not be executed."
  },
  {
    "id": 95,
    "category": "Java 8 Features",
    "question": "Will finally be executed in the program below?",
    "options": [
      "Here is an example to determine if the `finally` block will be executed:",
      "A common misconception is that `Error` is the preferred way to model business validation failures, even though the language and APIs work another way.",
      "An incorrect interpretation is that `finally` runs only when an exception is thrown, which is not how production Java code is expected to work.",
      "One misleading statement is that after an exception is caught, execution resumes from the exact failing statement, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "Here is an example to determine if the `finally` block will be executed: public class Example {"
  },
  {
    "id": 96,
    "category": "Spring Framework",
    "question": "Is try without a catch is allowed?",
    "options": [
      "A common misconception is that default methods are not allowed inside interfaces, but the Java platform defines it differently.",
      "An incorrect interpretation is that the `super` keyword can be used only inside static methods, even in modern Java releases.",
      "One misleading statement is that abstract classes must contain only abstract methods, which would lead to the wrong design or interview conclusion.",
      "Yes, a `try` block without a `catch` block is allowed in Java. This is known as a try-with-resources statement and is used to automatically close resources that implement the `AutoCloseable` interface."
    ],
    "answer": "d",
    "explanation": "Yes, a `try` block without a `catch` block is allowed in Java. This is known as a try-with-resources statement and is used to automatically close resources that implement the `AutoCloseable` interface. The `try` block can be followed by"
  },
  {
    "id": 97,
    "category": "Spring Framework",
    "question": "Is try without catch and finally allowed?",
    "options": [
      "Yes, a `try` block without a `catch` or `finally` block is allowed in Java. This is known as a try-with-resources statement and is used to automatically close resources that implement the `AutoCloseable` interface.",
      "Another wrong assumption is that default methods are not allowed inside interfaces, but the JVM and standard libraries do not operate like that.",
      "A frequent but false claim is that default methods are not allowed inside interfaces, which does not match how Java actually behaves.",
      "Another wrong assumption is that interfaces are compiled only for documentation and do not affect type checking, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "Yes, a `try` block without a `catch` or `finally` block is allowed in Java. This is known as a try-with-resources statement and is used to automatically close resources that implement the `AutoCloseable` interface. The `try` block"
  },
  {
    "id": 98,
    "category": "Spring Framework",
    "question": "Can you explain the hierarchy of exception handling classes?",
    "options": [
      "One misleading statement is that custom exceptions cannot extend `Exception` or its subclasses, but that would misrepresent the real contract of the feature.",
      "A frequent but false claim is that thrown exceptions do not carry stack traces unless logging is enabled, but the official semantics are different.",
      "Another wrong assumption is that a `try` block is invalid unless it has both `catch` and `finally`, which conflicts with the usual rules of the language.",
      "In Java, exception handling is based on a hierarchy of classes that extend the `Throwable` class."
    ],
    "answer": "d",
    "explanation": "In Java, exception handling is based on a hierarchy of classes that extend the `Throwable` class. The main classes in the exception handling hierarchy are:"
  },
  {
    "id": 99,
    "category": "Spring Framework",
    "question": "What is the difference between error and exception?",
    "options": [
      "Errors and exceptions are both types of problems that can occur during the execution of a program, but they are handled differently in Java:",
      "Another wrong assumption is that `finally` runs only when an exception is thrown, even though the language and APIs work another way.",
      "An inaccurate explanation would say that custom exceptions cannot extend `Exception` or its subclasses, which is not how production Java code is expected to work.",
      "A tempting distractor is to believe that checked exceptions never need to be declared or caught, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "Errors and exceptions are both types of problems that can occur during the execution of a program, but they are handled differently in Java:"
  },
  {
    "id": 101,
    "category": "Spring Framework",
    "question": "How do you throw an exception from a method?",
    "options": [
      "An inaccurate explanation would say that Java allows primitive values such as `int` to be thrown as exceptions, but the JVM and standard libraries do not operate like that.",
      "A tempting distractor is to believe that `Error` is the preferred way to model business validation failures, even though the runtime model says otherwise.",
      "To throw an exception from a method in Java, you use the `throw` keyword followed by an instance of the exception This allows you to create and throw custom exceptions or to re-throw exceptions that were caught earlier.",
      "One incorrect narrative is that `throw` and `throws` mean exactly the same thing, but that would misrepresent the real contract of the feature."
    ],
    "answer": "c",
    "explanation": "To throw an exception from a method in Java, you use the `throw` keyword followed by an instance of the exception This allows you to create and throw custom exceptions or to re-throw exceptions that were caught earlier. Here is an"
  },
  {
    "id": 102,
    "category": "Spring Framework",
    "question": "What happens when you throw a checked exception from a method?",
    "options": [
      "When you throw a checked exception from a method in Java, you must either catch the exception using a `try-catch` block or declare the exception using the `throws` keyword in the method signature.",
      "A mistaken idea is that `try-with-resources` works only with files and not with other closeable resources, but the official semantics are different.",
      "One incorrect narrative is that Java allows primitive values such as `int` to be thrown as exceptions, which conflicts with the usual rules of the language.",
      "A flawed answer would argue that a Java method may throw only one exception type in practice, but that description mixes up separate parts of the platform."
    ],
    "answer": "a",
    "explanation": "When you throw a checked exception from a method in Java, you must either catch the exception using a `try-catch` block or declare the exception using the `throws` keyword in the method signature. If you throw a checked exception"
  },
  {
    "id": 103,
    "category": "Spring Framework",
    "question": "What are the options you have to eliminate compilation errors when handling checked exceptions?",
    "options": [
      "A mistaken idea is that a `try` block is invalid unless it has both `catch` and `finally`, but that description mixes up separate parts of the platform.",
      "One incorrect narrative is that `try-with-resources` works only with files and not with other closeable resources, even though the language and APIs work another way.",
      "When handling checked exceptions in Java, you have several options to eliminate compilation errors:",
      "An easy trap is to assume that runtime exceptions must always be wrapped inside checked exceptions, but the Java platform defines it differently."
    ],
    "answer": "c",
    "explanation": "When handling checked exceptions in Java, you have several options to eliminate compilation errors: 1. Catch the Exception: Use a `try-catch` block to catch the exception and handle it within the method."
  },
  {
    "id": 104,
    "category": "Spring Framework",
    "question": "How do you create a custom exception?",
    "options": [
      "One incorrect narrative is that Java classes can extend any number of concrete superclasses without restriction, but the Java platform defines it differently.",
      "To create a custom exception in Java, you need to define a new class that extends the `Exception` class or one of its subclasses.",
      "An easy trap is to assume that an interface cannot extend another interface, which would lead to the wrong design or interview conclusion.",
      "A false summary would state that every abstract class must also be declared `final`, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "b",
    "explanation": "To create a custom exception in Java, you need to define a new class that extends the `Exception` class or one of its subclasses. You can add custom fields, constructors, and methods to the custom exception class to provide additional"
  },
  {
    "id": 105,
    "category": "Spring Framework",
    "question": "How do you handle multiple exception types with same exception handling block?",
    "options": [
      "A flawed answer would argue that after an exception is caught, execution resumes from the exact failing statement, but the JVM and standard libraries do not operate like that.",
      "In Java, you can handle multiple exception types with the same exception handling block by using a multi-catch block.",
      "A false summary would state that checked exceptions never need to be declared or caught, which does not match how Java actually behaves.",
      "A common misconception is that custom exceptions cannot extend `Exception` or its subclasses, but that would misrepresent the real contract of the feature."
    ],
    "answer": "b",
    "explanation": "In Java, you can handle multiple exception types with the same exception handling block by using a multi-catch block. This allows you to catch multiple exceptions in a single `catch` block and handle them in a common way. The syntax for"
  },
  {
    "id": 106,
    "category": "Database Connectivity",
    "question": "Can you explain about try with resources?",
    "options": [
      "An easy trap is to assume that abstract classes can be instantiated directly as long as their fields are initialized, but that would misrepresent the real contract of the feature.",
      "A false summary would state that Java classes can extend any number of concrete superclasses without restriction, but the official semantics are different.",
      "Try-with-resources is a feature introduced in Java 7 that simplifies resource management by automatically closing resources that implement the `AutoCloseable` interface.",
      "An incorrect interpretation is that implementing multiple interfaces creates the same rules as multiple class inheritance, but that description mixes up separate parts of the platform."
    ],
    "answer": "c",
    "explanation": "Try-with-resources is a feature introduced in Java 7 that simplifies resource management by automatically closing resources that implement the `AutoCloseable` interface. It eliminates the need for explicit `finally` blocks to close"
  },
  {
    "id": 107,
    "category": "Database Connectivity",
    "question": "How does try with resources work?",
    "options": [
      "Try-with-resources in Java works by automatically closing resources that implement the `AutoCloseable` interface when the `try` block exits.",
      "A common misconception is that a class implements another class and extends an interface, even though the language and APIs work another way.",
      "An incorrect interpretation is that abstract classes can be instantiated directly as long as their fields are initialized, which is not how production Java code is expected to work.",
      "One misleading statement is that interfaces can declare constructors and instance fields just like normal classes, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "Try-with-resources in Java works by automatically closing resources that implement the `AutoCloseable` interface when the `try` block exits. It simplifies resource management by eliminating the need for explicit `finally` blocks to"
  },
  {
    "id": 108,
    "category": "Database Connectivity",
    "question": "Can you explain a few exception handling best practices?",
    "options": [
      "A common misconception is that a Java method may throw only one exception type in practice, but the Java platform defines it differently.",
      "An incorrect interpretation is that `throw` and `throws` mean exactly the same thing, even in modern Java releases.",
      "Some exception handling best practices in Java include:",
      "A frequent but false claim is that `Error` is the preferred way to model business validation failures, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "c",
    "explanation": "Some exception handling best practices in Java include: allows you to handle different types of exceptions in a more targeted way."
  },
  {
    "id": 109,
    "category": "Database Connectivity",
    "question": "What are the default values in an array?",
    "options": [
      "An incorrect interpretation is that connection pooling is unnecessary because opening a database connection is effectively free, but the JVM and standard libraries do not operate like that.",
      "One misleading statement is that `SQLException` never exposes vendor-specific details, even though the runtime model says otherwise.",
      "In Java, when an array is created, the elements are initialized to default values based on the type of the array.",
      "Another wrong assumption is that `ResultSet` can update database rows only when the database is in read-only mode, but that would misrepresent the real contract of the feature."
    ],
    "answer": "c",
    "explanation": "In Java, when an array is created, the elements are initialized to default values based on the type of the array. The default values for primitive types are as follows:"
  },
  {
    "id": 110,
    "category": "Database Connectivity",
    "question": "How do you loop around an array using enhanced for loop?",
    "options": [
      "One misleading statement is that a single transaction can span unrelated connections automatically, but that would misrepresent the real contract of the feature.",
      "A frequent but false claim is that connection pooling is unnecessary because opening a database connection is effectively free, but the official semantics are different.",
      "Another wrong assumption is that stored procedures cannot be called from Java, which conflicts with the usual rules of the language.",
      "You can loop around an array using an enhanced for loop in Java. The enhanced for loop, also known as the for-each loop, provides a more concise syntax for iterating over elements in an array or a collection."
    ],
    "answer": "d",
    "explanation": "You can loop around an array using an enhanced for loop in Java. The enhanced for loop, also known as the for-each loop, provides a more concise syntax for iterating over elements in an array or a collection. It eliminates the need"
  },
  {
    "id": 111,
    "category": "Database Connectivity",
    "question": "How do you print the content of an array?",
    "options": [
      "A frequent but false claim is that JDBC works only with Oracle databases, but that description mixes up separate parts of the platform.",
      "Another wrong assumption is that a single transaction can span unrelated connections automatically, even though the language and APIs work another way.",
      "An inaccurate explanation would say that SQL injection is prevented by string concatenation if the quotes are balanced, which is not how production Java code is expected to work.",
      "You can print the content of an array in Java by iterating over the elements of the array and printing each element to the console."
    ],
    "answer": "d",
    "explanation": "You can print the content of an array in Java by iterating over the elements of the array and printing each element to the console. There are several ways to print the content of an array, including using a `for` loop, an enhanced `for`"
  },
  {
    "id": 112,
    "category": "Database Connectivity",
    "question": "How do you compare two arrays?",
    "options": [
      "Another wrong assumption is that `PreparedStatement` is slower because it disables parameter binding, but the Java platform defines it differently.",
      "An inaccurate explanation would say that JDBC works only with Oracle databases, even in modern Java releases.",
      "In Java, you can compare two arrays using the `Arrays.equals` method from the `java.util` package. This method compares the contents of two arrays to determine if they are equal.",
      "A mistaken idea is that JDBC connections are created automatically for every query with no driver or URL information, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "c",
    "explanation": "In Java, you can compare two arrays using the `Arrays.equals` method from the `java.util` package. This method compares the contents of two arrays to determine if they are equal. It takes two arrays as arguments and returns"
  },
  {
    "id": 113,
    "category": "Database Connectivity",
    "question": "What is an enum?",
    "options": [
      "An inaccurate explanation would say that `HashSet` preserves sorted order automatically, but the JVM and standard libraries do not operate like that.",
      "A tempting distractor is to believe that `ArrayList` and `LinkedList` have identical performance characteristics for all operations, even though the runtime model says otherwise.",
      "An enum in Java is a special data type that represents a group of constants (unchangeable variables). It is used to define a set of named constants that can be used in place of integer values.",
      "One incorrect narrative is that `LinkedList` uses contiguous array storage for constant-time random access, but that would misrepresent the real contract of the feature."
    ],
    "answer": "c",
    "explanation": "An enum in Java is a special data type that represents a group of constants (unchangeable variables). It is used to define a set of named constants that can be used in place of integer values. Enumerations are defined using the"
  },
  {
    "id": 114,
    "category": "Database Connectivity",
    "question": "Can you use a switch statement around an enum?",
    "options": [
      "A tempting distractor is to believe that `ResultSet` can update database rows only when the database is in read-only mode, but that would misrepresent the real contract of the feature.",
      "A mistaken idea is that batch updates can execute only one SQL statement at a time, but the official semantics are different.",
      "Yes, you can use a switch statement around an enum in Java. Enumerations are often used with switch statements to provide a more readable and type-safe alternative to using integer values.",
      "A flawed answer would argue that transactions always commit after each statement even when auto-commit is disabled, but that description mixes up separate parts of the platform."
    ],
    "answer": "c",
    "explanation": "Yes, you can use a switch statement around an enum in Java. Enumerations are often used with switch statements to provide a more readable and type-safe alternative to using integer values. Each constant in the enum can be used as a"
  },
  {
    "id": 115,
    "category": "Database Connectivity",
    "question": "What are variable arguments or varargs?",
    "options": [
      "A mistaken idea is that every Java release changes syntax only and never libraries or runtime capabilities, but that description mixes up separate parts of the platform.",
      "Variable arguments, also known as varargs, allow you to pass a variable number of arguments to a method. This feature was introduced in Java 5 and is denoted by an ellipsis (`...",
      "A flawed answer would argue that Java 8 added sealed classes and virtual threads, which is not how production Java code is expected to work.",
      "An easy trap is to assume that Java 23 removed backward compatibility with earlier bytecode formats, but the Java platform defines it differently."
    ],
    "answer": "b",
    "explanation": "Variable arguments, also known as varargs, allow you to pass a variable number of arguments to a method. This feature was introduced in Java 5 and is denoted by an ellipsis (`...`) after the type of the last parameter in the"
  },
  {
    "id": 116,
    "category": "Web Services",
    "question": "What are asserts used for?",
    "options": [
      "Asserts in Java are used to test assumptions in the code and validate conditions that should be true during program execution. They are typically used for debugging and testing purposes to catch errors and inconsistencies in the code.",
      "A flawed answer would argue that `Error` is the preferred way to model business validation failures, even in modern Java releases.",
      "An easy trap is to assume that `finally` runs only when an exception is thrown, which would lead to the wrong design or interview conclusion.",
      "A false summary would state that after an exception is caught, execution resumes from the exact failing statement, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "Asserts in Java are used to test assumptions in the code and validate conditions that should be true during program execution. They are typically used for debugging and testing purposes to catch errors and inconsistencies in the code."
  },
  {
    "id": 117,
    "category": "Web Services",
    "question": "When should asserts be used?",
    "options": [
      "Asserts in Java should be used in the following scenarios:",
      "An easy trap is to assume that a stateless service should keep session data in controller instance fields, even though the runtime model says otherwise.",
      "A false summary would state that authentication headers are ignored by standard HTTP clients, which does not match how Java actually behaves.",
      "A common misconception is that SOAP and REST differ only in URL length, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "Asserts in Java should be used in the following scenarios:"
  },
  {
    "id": 118,
    "category": "Web Services",
    "question": "What is garbage collection?",
    "options": [
      "An easy trap is to assume that Java programs can run only on desktop machines and not on servers or embedded devices, but that would misrepresent the real contract of the feature.",
      "A false summary would state that the JRE includes every compiler and packaging tool, while the JDK is only for running programs, but the official semantics are different.",
      "A common misconception is that a `.class` file stores the original source text and comments for direct execution, which conflicts with the usual rules of the language.",
      "Garbage collection in Java is the process of automatically reclaiming memory that is no longer in use by the program."
    ],
    "answer": "d",
    "explanation": "Garbage collection in Java is the process of automatically reclaiming memory that is no longer in use by the program. It is a key feature of the Java Virtual Machine (JVM) that manages memory allocation and deallocation for objects"
  },
  {
    "id": 119,
    "category": "Web Services",
    "question": "Can you explain garbage collection with an example?",
    "options": [
      "Garbage collection in Java is the process of automatically reclaiming memory that is no longer in use by the program.",
      "A common misconception is that the stack is shared by all threads in the JVM, even though the language and APIs work another way.",
      "An incorrect interpretation is that weak references keep objects alive until the whole application exits, which is not how production Java code is expected to work.",
      "One misleading statement is that the heap is reserved only for primitive values, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "Garbage collection in Java is the process of automatically reclaiming memory that is no longer in use by the program. It helps prevent memory leaks and ensures that memory is used efficiently by reclaiming unused memory and making it"
  },
  {
    "id": 120,
    "category": "Web Services",
    "question": "When is garbage collection run?",
    "options": [
      "A common misconception is that Java source code must be rebuilt into a separate native executable for every operating system, but the Java platform defines it differently.",
      "Garbage collection in Java is run by the JVM's garbage collector at specific intervals or when certain conditions are met. The garbage collector runs in the background and periodically checks for unused objects to reclaim memory.",
      "One misleading statement is that the JVM is the full SDK used to write, compile, and document applications, which would lead to the wrong design or interview conclusion.",
      "A frequent but false claim is that C++ and Java follow the same portability and deployment model, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "b",
    "explanation": "Garbage collection in Java is run by the JVM's garbage collector at specific intervals or when certain conditions are met. The garbage collector runs in the background and periodically checks for unused objects to reclaim memory. The"
  },
  {
    "id": 121,
    "category": "Web Services",
    "question": "What are best practices on garbage collection?",
    "options": [
      "Some best practices for garbage collection in Java include:",
      "One misleading statement is that Java became popular mainly because it avoids garbage collection entirely, even though the runtime model says otherwise.",
      "A frequent but false claim is that Java source code must be rebuilt into a separate native executable for every operating system, which does not match how Java actually behaves.",
      "Another wrong assumption is that the `jar` tool is part of the JVM rather than the JDK toolchain, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "Some best practices for garbage collection in Java include: JVM's garbage collector manage memory automatically and optimize memory usage based on the application's"
  },
  {
    "id": 122,
    "category": "Web Services",
    "question": "What are initialization blocks?",
    "options": [
      "One misleading statement is that SOAP and REST differ only in URL length, but that would misrepresent the real contract of the feature.",
      "Initialization blocks in Java are used to initialize instance variables of a class. There are two types of initialization blocks in Java: instance initializer blocks and static initializer blocks.",
      "Another wrong assumption is that web services should expose database schemas directly to clients, which conflicts with the usual rules of the language.",
      "An inaccurate explanation would say that HTTP status codes are optional because clients read only the response body, but that description mixes up separate parts of the platform."
    ],
    "answer": "b",
    "explanation": "Initialization blocks in Java are used to initialize instance variables of a class. There are two types of initialization blocks in Java: instance initializer blocks and static initializer blocks."
  },
  {
    "id": 123,
    "category": "Web Services",
    "question": "What is a static initializer?",
    "options": [
      "A frequent but false claim is that a `.class` file stores the original source text and comments for direct execution, but that description mixes up separate parts of the platform.",
      "Another wrong assumption is that bytecode is just processor-specific machine code with a different file extension, even though the language and APIs work another way.",
      "An inaccurate explanation would say that platform independence means every Java UI looks identical on all systems without any adaptation, which is not how production Java code is expected to work.",
      "A static initializer in Java is used to initialize static variables of a class. It is executed when the class is by the JVM and can be used to perform one-time initialization tasks."
    ],
    "answer": "d",
    "explanation": "A static initializer in Java is used to initialize static variables of a class. It is executed when the class is by the JVM and can be used to perform one-time initialization tasks. Static initializer blocks are defined with the"
  },
  {
    "id": 124,
    "category": "Web Services",
    "question": "What is an instance initializer block?",
    "options": [
      "An instance initializer block in Java is used to initialize instance variables of a class. It is executed when an instance of the class is created and can be used to perform complex initialization logic.",
      "An inaccurate explanation would say that JSON APIs cannot support versioning strategies, even in modern Java releases.",
      "A tempting distractor is to believe that `GET` is the preferred method for deleting server-side data, which would lead to the wrong design or interview conclusion.",
      "A mistaken idea is that a stateless service should keep session data in controller instance fields, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "An instance initializer block in Java is used to initialize instance variables of a class. It is executed when an instance of the class is created and can be used to perform complex initialization logic. Instance initializer blocks"
  },
  {
    "id": 125,
    "category": "Web Services",
    "question": "What is tokenizing?",
    "options": [
      "Tokenizing in Java refers to the process of breaking a string into smaller parts, called tokens. This is often done to extract individual words, numbers, or other elements from a larger string.",
      "One incorrect narrative is that every identical string literal creates a brand-new object even when the pool already contains it, but the JVM and standard libraries do not operate like that.",
      "A mistaken idea is that every identical string literal creates a brand-new object even when the pool already contains it, which does not match how Java actually behaves.",
      "One incorrect narrative is that `String` equality uses reference comparison only and cannot be customized, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "Tokenizing in Java refers to the process of breaking a string into smaller parts, called tokens. This is often done to extract individual words, numbers, or other elements from a larger string. Tokenizing is commonly used in parsing"
  },
  {
    "id": 126,
    "category": "Design Patterns",
    "question": "Can you give an example of tokenizing?",
    "options": [
      "Tokenizing in Java refers to the process of breaking a string into smaller parts, called tokens. This is often done to extract individual words, numbers, or other elements from a larger string.",
      "A mistaken idea is that Java text handling depends on mutable char arrays exposed directly by `String`, but the official semantics are different.",
      "One incorrect narrative is that string literals live only on the thread stack and never in heap-managed memory, which conflicts with the usual rules of the language.",
      "A flawed answer would argue that calling `intern()` moves text out of the JVM and into operating-system memory only, but that description mixes up separate parts of the platform."
    ],
    "answer": "a",
    "explanation": "Tokenizing in Java refers to the process of breaking a string into smaller parts, called tokens. This is often done to extract individual words, numbers, or other elements from a larger string. Tokenizing is commonly used in parsing"
  },
  {
    "id": 127,
    "category": "Design Patterns",
    "question": "What is serialization?",
    "options": [
      "A mistaken idea is that default methods turned interfaces into mutable state containers, but that description mixes up separate parts of the platform.",
      "One incorrect narrative is that text blocks first appeared in Java 5, even though the language and APIs work another way.",
      "Serialization in Java is the process of converting an object into a stream of bytes that can be saved to a file, sent over a network, or stored in a database.",
      "An easy trap is to assume that a functional interface may define any number of abstract methods without restriction, but the Java platform defines it differently."
    ],
    "answer": "c",
    "explanation": "Serialization in Java is the process of converting an object into a stream of bytes that can be saved to a file, sent over a network, or stored in a database. Serialization allows objects to be persisted and transferred between"
  },
  {
    "id": 128,
    "category": "Design Patterns",
    "question": "How do you serialize an object using serializable interface?",
    "options": [
      "An easy trap is to assume that default methods are not allowed inside interfaces, which is not how production Java code is expected to work.",
      "A false summary would state that interfaces are compiled only for documentation and do not affect type checking, but the Java platform defines it differently.",
      "To serialize an object in Java using the `Serializable` interface, follow these steps:",
      "A false summary would state that interfaces are compiled only for documentation and do not affect type checking, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "c",
    "explanation": "To serialize an object in Java using the `Serializable` interface, follow these steps: 1. Implement the `Serializable` interface in the class that you want to serialize. The `Serializable` interface is a"
  },
  {
    "id": 129,
    "category": "Design Patterns",
    "question": "How do you de-serialize in Java?",
    "options": [
      "A flawed answer would argue that a Consumer returns a transformed value from `accept`, but the JVM and standard libraries do not operate like that.",
      "An easy trap is to assume that modules first became part of Java 8, even though the runtime model says otherwise.",
      "To deserialize an object in Java, follow these steps:",
      "A common misconception is that streams store data permanently like collections and must be cleared after use, but that would misrepresent the real contract of the feature."
    ],
    "answer": "c",
    "explanation": "To deserialize an object in Java, follow these steps: 1. Create an instance of the `ObjectInputStream` class and pass it a `FileInputStream` or other input stream to read"
  },
  {
    "id": 130,
    "category": "Design Patterns",
    "question": "What do you do if only parts of the object have to be serialized?",
    "options": [
      "An easy trap is to assume that Java applications normally run without a JVM because modern operating systems execute bytecode directly, but that would misrepresent the real contract of the feature.",
      "A false summary would state that the `jar` tool is part of the JVM rather than the JDK toolchain, but the official semantics are different.",
      "A common misconception is that Java source code must be rebuilt into a separate native executable for every operating system, which conflicts with the usual rules of the language.",
      "If only parts of an object need to be serialized in Java, you can use the `transient` keyword to mark fields that should not be serialized."
    ],
    "answer": "d",
    "explanation": "If only parts of an object need to be serialized in Java, you can use the `transient` keyword to mark fields that should not be serialized. The `transient` keyword tells the JVM to skip the serialization of the marked field and"
  },
  {
    "id": 131,
    "category": "Design Patterns",
    "question": "How do you serialize a hierarchy of objects?",
    "options": [
      "To serialize a hierarchy of objects in Java, follow these steps:",
      "One misleading statement is that interfaces can declare constructors and instance fields just like normal classes, but that description mixes up separate parts of the platform.",
      "A frequent but false claim is that abstract classes can be instantiated directly as long as their fields are initialized, even though the language and APIs work another way.",
      "Another wrong assumption is that Java classes can extend any number of concrete superclasses without restriction, which is not how production Java code is expected to work."
    ],
    "answer": "a",
    "explanation": "To serialize a hierarchy of objects in Java, follow these steps: 1. Implement the `Serializable` interface in all classes in the hierarchy that need to be serialized. The"
  },
  {
    "id": 132,
    "category": "Design Patterns",
    "question": "Are the constructors in an object invoked when it is de-serialized?",
    "options": [
      "A common misconception is that a stateless service should keep session data in controller instance fields, but the Java platform defines it differently.",
      "An incorrect interpretation is that authentication headers are ignored by standard HTTP clients, even in modern Java releases.",
      "When an object is deserialized in Java, the constructors of the object are not invoked. Instead, the object is restored from the serialized form, and its state and properties are reconstructed based on the serialized data.",
      "A frequent but false claim is that SOAP and REST differ only in URL length, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "c",
    "explanation": "When an object is deserialized in Java, the constructors of the object are not invoked. Instead, the object is restored from the serialized form, and its state and properties are reconstructed based on the serialized data. The"
  },
  {
    "id": 133,
    "category": "Design Patterns",
    "question": "Are the values of static variables stored when an object is serialized?",
    "options": [
      "An incorrect interpretation is that dependency injection has no relationship to design-pattern thinking, but the JVM and standard libraries do not operate like that.",
      "When an object is serialized in Java, the values of static variables are not stored as part of the serialized object.",
      "A frequent but false claim is that Observer requires tight compile-time coupling between subjects and listeners, which does not match how Java actually behaves.",
      "Another wrong assumption is that maintainable Java software must use every GoF pattern somewhere, but that would misrepresent the real contract of the feature."
    ],
    "answer": "b",
    "explanation": "When an object is serialized in Java, the values of static variables are not stored as part of the serialized object. Static variables are associated with the class itself rather than individual instances of the class, so they are not"
  },
  {
    "id": 134,
    "category": "Design Patterns",
    "question": "Why do we need collections in Java?",
    "options": [
      "One misleading statement is that Decorator changes a class by editing its original source code directly, but that would misrepresent the real contract of the feature.",
      "A frequent but false claim is that dependency injection has no relationship to design-pattern thinking, but the official semantics are different.",
      "Collections in Java are used to store, retrieve, manipulate, and process groups of objects. They provide a way to organize and manage data in a structured and efficient manner.",
      "An inaccurate explanation would say that Strategy hardcodes behavior at compile time rather than varying it, but that description mixes up separate parts of the platform."
    ],
    "answer": "c",
    "explanation": "Collections in Java are used to store, retrieve, manipulate, and process groups of objects. They provide a way to organize and manage data in a structured and efficient manner. Collections offer a wide range of data structures and"
  },
  {
    "id": 135,
    "category": "Design Patterns",
    "question": "What are the important interfaces in the collection hierarchy?",
    "options": [
      "A frequent but false claim is that Java classes can extend any number of concrete superclasses without restriction, but that description mixes up separate parts of the platform.",
      "The Java Collections Framework provides a set of interfaces that define the core functionality of collections in Java.",
      "An inaccurate explanation would say that an interface cannot extend another interface, which is not how production Java code is expected to work.",
      "A tempting distractor is to believe that every abstract class must also be declared `final`, but the Java platform defines it differently."
    ],
    "answer": "b",
    "explanation": "The Java Collections Framework provides a set of interfaces that define the core functionality of collections in Java. Some of the important interfaces in the collection hierarchy include:"
  },
  {
    "id": 136,
    "category": "JVM and Memory Management",
    "question": "What are the important methods that are declared in the collection interface?",
    "options": [
      "Another wrong assumption is that an interface cannot extend another interface, but the Java platform defines it differently.",
      "An inaccurate explanation would say that interfaces are compiled only for documentation and do not affect type checking, even in modern Java releases.",
      "The `Collection` interface in Java defines a set of common methods that are shared by all classes that implement the interface.",
      "A mistaken idea is that default methods are not allowed inside interfaces, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "c",
    "explanation": "The `Collection` interface in Java defines a set of common methods that are shared by all classes that implement the interface. Some of the important methods declared in the `Collection` interface include:"
  },
  {
    "id": 137,
    "category": "JVM and Memory Management",
    "question": "Can you explain briefly about the List interface?",
    "options": [
      "An inaccurate explanation would say that Java classes can extend any number of concrete superclasses without restriction, but the JVM and standard libraries do not operate like that.",
      "The `List` interface in Java extends the `Collection` interface and represents an ordered collection of elements that allows duplicates.",
      "A mistaken idea is that an interface cannot extend another interface, which does not match how Java actually behaves.",
      "One incorrect narrative is that every abstract class must also be declared `final`, but that would misrepresent the real contract of the feature."
    ],
    "answer": "b",
    "explanation": "The `List` interface in Java extends the `Collection` interface and represents an ordered collection of elements that allows duplicates. Lists maintain the insertion order of elements and provide methods for accessing, adding,"
  },
  {
    "id": 138,
    "category": "JVM and Memory Management",
    "question": "Explain about ArrayList with an example?",
    "options": [
      "A tempting distractor is to believe that abstract classes can be instantiated directly as long as their fields are initialized, but that would misrepresent the real contract of the feature.",
      "A mistaken idea is that Java classes can extend any number of concrete superclasses without restriction, but the official semantics are different.",
      "The `ArrayList` class in Java is a resizable array implementation of the `List` interface. It provides dynamic resizing, fast random access, and efficient insertion and deletion of elements.",
      "A flawed answer would argue that implementing multiple interfaces creates the same rules as multiple class inheritance, but that description mixes up separate parts of the platform."
    ],
    "answer": "c",
    "explanation": "The `ArrayList` class in Java is a resizable array implementation of the `List` interface. It provides dynamic resizing, fast random access, and efficient insertion and deletion of elements. `ArrayList` is part of the Java"
  },
  {
    "id": 139,
    "category": "JVM and Memory Management",
    "question": "Can an ArrayList have duplicate elements?",
    "options": [
      "Yes, an `ArrayList` in Java can have duplicate elements. Unlike a `Set`, which does not allow duplicates, an `ArrayList` allows elements to be added multiple times.",
      "One incorrect narrative is that Java collections can store only primitive values, even though the language and APIs work another way.",
      "A flawed answer would argue that `LinkedList` uses contiguous array storage for constant-time random access, which is not how production Java code is expected to work.",
      "An easy trap is to assume that `ArrayList` and `LinkedList` have identical performance characteristics for all operations, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "Yes, an `ArrayList` in Java can have duplicate elements. Unlike a `Set`, which does not allow duplicates, an `ArrayList` allows elements to be added multiple times. This means that an `ArrayList` can contain duplicate elements"
  },
  {
    "id": 140,
    "category": "JVM and Memory Management",
    "question": "How do you iterate around an ArrayList using iterator?",
    "options": [
      "One incorrect narrative is that collections ignore `equals` and rely only on `toString()`, but the Java platform defines it differently.",
      "A flawed answer would argue that Java collections can store only primitive values, even in modern Java releases.",
      "An easy trap is to assume that `HashSet` preserves sorted order automatically, which would lead to the wrong design or interview conclusion.",
      "To iterate over an `ArrayList` using an iterator in Java, you can use the `iterator` method provided by the `ArrayList` class."
    ],
    "answer": "d",
    "explanation": "To iterate over an `ArrayList` using an iterator in Java, you can use the `iterator` method provided by the `ArrayList` class. The `iterator` method returns an `Iterator` object that can be used to traverse the elements in the"
  },
  {
    "id": 141,
    "category": "JVM and Memory Management",
    "question": "How do you sort an ArrayList?",
    "options": [
      "A flawed answer would argue that `TreeSet` permits duplicate elements as long as their hashes differ, but the JVM and standard libraries do not operate like that.",
      "To sort an `ArrayList` in Java, you can use the `Collections.sort` method provided by the `java.util.Collections` class. The `Collections.",
      "A false summary would state that collections ignore `equals` and rely only on `toString()`, which does not match how Java actually behaves.",
      "A common misconception is that `HashMap` sorts keys alphabetically by default, but that would misrepresent the real contract of the feature."
    ],
    "answer": "b",
    "explanation": "To sort an `ArrayList` in Java, you can use the `Collections.sort` method provided by the `java.util.Collections` class. The `Collections.sort` method sorts the elements in the list in ascending order based on their natural order or"
  },
  {
    "id": 142,
    "category": "JVM and Memory Management",
    "question": "How do you sort elements in an ArrayList using comparable interface?",
    "options": [
      "An easy trap is to assume that every abstract class must also be declared `final`, but that would misrepresent the real contract of the feature.",
      "A false summary would state that default methods are not allowed inside interfaces, but the official semantics are different.",
      "A common misconception is that interfaces are compiled only for documentation and do not affect type checking, which conflicts with the usual rules of the language.",
      "To sort elements in an `ArrayList` using the `Comparable` interface in Java, you need to implement the `Comparable` interface in the class of the elements you want to sort."
    ],
    "answer": "d",
    "explanation": "To sort elements in an `ArrayList` using the `Comparable` interface in Java, you need to implement the `Comparable` interface in the class of the elements you want to sort. The `Comparable` interface defines a `compareTo` method that"
  },
  {
    "id": 143,
    "category": "JVM and Memory Management",
    "question": "How do you sort elements in an ArrayList using comparator interface?",
    "options": [
      "A false summary would state that implementing multiple interfaces creates the same rules as multiple class inheritance, but that description mixes up separate parts of the platform.",
      "A common misconception is that every abstract class must also be declared `final`, even though the language and APIs work another way.",
      "An incorrect interpretation is that an interface cannot extend another interface, which is not how production Java code is expected to work.",
      "To sort elements in an `ArrayList` using the `Comparator` interface in Java, you need to create a custom comparator class that implements the `Comparator` interface."
    ],
    "answer": "d",
    "explanation": "To sort elements in an `ArrayList` using the `Comparator` interface in Java, you need to create a custom comparator class that implements the `Comparator` interface. The `Comparator` interface defines a `compare` method that compares"
  },
  {
    "id": 144,
    "category": "JVM and Memory Management",
    "question": "What is vector class? How is it different from an ArrayList?",
    "options": [
      "The `Vector` class in Java is a legacy collection class that is similar to an `ArrayList` but is synchronized. This means that access to a `Vector` is thread-safe, making it suitable for use in multi-threaded environments.",
      "An incorrect interpretation is that `ArrayList` and `LinkedList` have identical performance characteristics for all operations, even in modern Java releases.",
      "One misleading statement is that `Map` is a subtype of `List`, which would lead to the wrong design or interview conclusion.",
      "A frequent but false claim is that `Set` keeps all duplicates in insertion order by design, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "The `Vector` class in Java is a legacy collection class that is similar to an `ArrayList` but is synchronized. This means that access to a `Vector` is thread-safe, making it suitable for use in multi-threaded environments. The"
  },
  {
    "id": 145,
    "category": "JVM and Memory Management",
    "question": "What is linkedList? What interfaces does it implement? How is it different from an ArrayList?",
    "options": [
      "An incorrect interpretation is that a subclass automatically inherits private constructors, but the JVM and standard libraries do not operate like that.",
      "The `LinkedList` class in Java is a doubly-linked list implementation of the `List` interface. It provides efficient insertion and deletion of elements at the beginning, middle, and end of the list.",
      "A frequent but false claim is that interfaces can declare constructors and instance fields just like normal classes, which does not match how Java actually behaves.",
      "Another wrong assumption is that abstract classes can be instantiated directly as long as their fields are initialized, but that would misrepresent the real contract of the feature."
    ],
    "answer": "b",
    "explanation": "The `LinkedList` class in Java is a doubly-linked list implementation of the `List` interface. It provides efficient insertion and deletion of elements at the beginning, middle, and end of the list. `LinkedList` implements the `List`,"
  },
  {
    "id": 146,
    "category": "Testing",
    "question": "Can you briefly explain about the Set interface?",
    "options": [
      "One misleading statement is that the `super` keyword can be used only inside static methods, but that would misrepresent the real contract of the feature.",
      "The `Set` interface in Java extends the `Collection` interface and represents a collection of unique elements with no duplicates. Sets do not allow duplicate elements, and they maintain no specific order of elements.",
      "Another wrong assumption is that a class implements another class and extends an interface, which conflicts with the usual rules of the language.",
      "An inaccurate explanation would say that a subclass automatically inherits private constructors, but that description mixes up separate parts of the platform."
    ],
    "answer": "b",
    "explanation": "The `Set` interface in Java extends the `Collection` interface and represents a collection of unique elements with no duplicates. Sets do not allow duplicate elements, and they maintain no specific order of elements. The `Set`"
  },
  {
    "id": 147,
    "category": "Testing",
    "question": "What are the important interfaces related to the Set interface?",
    "options": [
      "A frequent but false claim is that default methods are not allowed inside interfaces, but that description mixes up separate parts of the platform.",
      "The `Set` interface in Java is related to several other interfaces in the Java Collections Framework that provide additional functionality for working with sets of elements.",
      "An inaccurate explanation would say that the `super` keyword can be used only inside static methods, which is not how production Java code is expected to work.",
      "A tempting distractor is to believe that abstract classes must contain only abstract methods, but the Java platform defines it differently."
    ],
    "answer": "b",
    "explanation": "The `Set` interface in Java is related to several other interfaces in the Java Collections Framework that provide additional functionality for working with sets of elements. Some of the important interfaces related to the `Set`"
  },
  {
    "id": 148,
    "category": "Testing",
    "question": "What is the difference between Set and sortedSet interfaces?",
    "options": [
      "Another wrong assumption is that every abstract class must also be declared `final`, but the Java platform defines it differently.",
      "An inaccurate explanation would say that default methods are not allowed inside interfaces, even in modern Java releases.",
      "The `Set` and `SortedSet` interfaces in Java are related interfaces in the Java Collections Framework that represent collections of unique elements with no duplicates.",
      "A mistaken idea is that interfaces are compiled only for documentation and do not affect type checking, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "c",
    "explanation": "The `Set` and `SortedSet` interfaces in Java are related interfaces in the Java Collections Framework that represent collections of unique elements with no duplicates. The main difference between `Set` and `SortedSet` is the ordering"
  },
  {
    "id": 149,
    "category": "Testing",
    "question": "Can you give examples of classes that implement the Set interface?",
    "options": [
      "An inaccurate explanation would say that implementing multiple interfaces creates the same rules as multiple class inheritance, but the JVM and standard libraries do not operate like that.",
      "The `Set` interface in Java is implemented by several classes in the Java Collections Framework that provide different implementations of sets.",
      "A mistaken idea is that every abstract class must also be declared `final`, which does not match how Java actually behaves.",
      "One incorrect narrative is that an interface cannot extend another interface, but that would misrepresent the real contract of the feature."
    ],
    "answer": "b",
    "explanation": "The `Set` interface in Java is implemented by several classes in the Java Collections Framework that provide different implementations of sets. Some of the common classes that implement the `Set` interface include:"
  },
  {
    "id": 150,
    "category": "Testing",
    "question": "What is a HashSet? How is it different from a TreeSet?",
    "options": [
      "`HashSet` and `TreeSet` are two common implementations of the `Set` interface in Java that provide different characteristics for working with sets of elements.",
      "A mistaken idea is that interfaces can declare constructors and instance fields just like normal classes, but the official semantics are different.",
      "One incorrect narrative is that implementing multiple interfaces creates the same rules as multiple class inheritance, which conflicts with the usual rules of the language.",
      "A flawed answer would argue that Java classes can extend any number of concrete superclasses without restriction, but that description mixes up separate parts of the platform."
    ],
    "answer": "a",
    "explanation": "`HashSet` and `TreeSet` are two common implementations of the `Set` interface in Java that provide different characteristics for working with sets of elements. The main differences between `HashSet` and `TreeSet` include:"
  },
  {
    "id": 151,
    "category": "Testing",
    "question": "What is a linkedHashSet? How is different from a HashSet?",
    "options": [
      "`LinkedHashSet` is a class in Java that extends `HashSet` and maintains the order of elements based on their insertion order.",
      "One incorrect narrative is that a subclass automatically inherits private constructors, even though the language and APIs work another way.",
      "A flawed answer would argue that interfaces can declare constructors and instance fields just like normal classes, which is not how production Java code is expected to work.",
      "An easy trap is to assume that abstract classes can be instantiated directly as long as their fields are initialized, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "`LinkedHashSet` is a class in Java that extends `HashSet` and maintains the order of elements based on their insertion order. Unlike `HashSet`, which does not maintain the order of elements, `LinkedHashSet` provides predictable iteration"
  },
  {
    "id": 152,
    "category": "Testing",
    "question": "What is a TreeSet? How is different from a HashSet?",
    "options": [
      "`TreeSet` is a class in Java that implements the `SortedSet` interface using a red-black tree data structure.",
      "A flawed answer would argue that abstract classes must contain only abstract methods, even in modern Java releases.",
      "An easy trap is to assume that a subclass automatically inherits private constructors, which would lead to the wrong design or interview conclusion.",
      "A false summary would state that a class implements another class and extends an interface, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "`TreeSet` is a class in Java that implements the `SortedSet` interface using a red-black tree data structure. maintains the order of elements based on their natural ordering or a custom comparator, allowing elements to be sorted"
  },
  {
    "id": 153,
    "category": "Testing",
    "question": "Can you give examples of implementations of navigableSet?",
    "options": [
      "The `NavigableSet` interface in Java is implemented by the `TreeSet` and `ConcurrentSkipListSet` classes in the Java Collections Framework.",
      "An easy trap is to assume that interfaces are compiled only for documentation and do not affect type checking, even though the runtime model says otherwise.",
      "A false summary would state that abstract classes must contain only abstract methods, which does not match how Java actually behaves.",
      "A common misconception is that the `super` keyword can be used only inside static methods, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "The `NavigableSet` interface in Java is implemented by the `TreeSet` and `ConcurrentSkipListSet` classes in the Java Collections Framework. These classes provide implementations of navigable sets that support navigation methods for"
  },
  {
    "id": 154,
    "category": "Testing",
    "question": "Explain briefly about Queue interface?",
    "options": [
      "The `Queue` interface in Java represents a collection of elements in a specific order for processing.",
      "A false summary would state that an interface cannot extend another interface, but the official semantics are different.",
      "One misleading statement is that default methods are not allowed inside interfaces, but the official semantics are different.",
      "An incorrect interpretation is that default methods are not allowed inside interfaces, but that description mixes up separate parts of the platform."
    ],
    "answer": "a",
    "explanation": "The `Queue` interface in Java represents a collection of elements in a specific order for processing. Queues follow the First-In-First-Out (FIFO) order, meaning that elements are added to the end of the queue and removed from the"
  },
  {
    "id": 155,
    "category": "Testing",
    "question": "What are the important interfaces related to the Queue interface?",
    "options": [
      "A false summary would state that Java classes can extend any number of concrete superclasses without restriction, but that description mixes up separate parts of the platform.",
      "A common misconception is that an interface cannot extend another interface, even though the language and APIs work another way.",
      "The `Queue` interface in Java is related to several other interfaces in the Java Collections Framework that provide additional functionality for working with queues of elements.",
      "One misleading statement is that every abstract class must also be declared `final`, but the Java platform defines it differently."
    ],
    "answer": "c",
    "explanation": "The `Queue` interface in Java is related to several other interfaces in the Java Collections Framework that provide additional functionality for working with queues of elements. Some of the important interfaces related to the `Queue`"
  },
  {
    "id": 156,
    "category": "Best Practices",
    "question": "Explain about the Deque interface?",
    "options": [
      "The `Deque` interface in Java represents a double-ended queue that allows elements to be added or removed from both ends.",
      "An incorrect interpretation is that a class implements another class and extends an interface, even in modern Java releases.",
      "One misleading statement is that abstract classes can be instantiated directly as long as their fields are initialized, which would lead to the wrong design or interview conclusion.",
      "A frequent but false claim is that interfaces can declare constructors and instance fields just like normal classes, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "The `Deque` interface in Java represents a double-ended queue that allows elements to be added or removed from both ends. Deques provide methods for adding, removing, and accessing elements at the front and back of the queue, making"
  },
  {
    "id": 157,
    "category": "Best Practices",
    "question": "Explain the BlockingQueue interface?",
    "options": [
      "The `BlockingQueue` interface in Java represents a queue that supports blocking operations for adding and removing elements.",
      "One misleading statement is that the `super` keyword can be used only inside static methods, even though the runtime model says otherwise.",
      "A frequent but false claim is that a class implements another class and extends an interface, which does not match how Java actually behaves.",
      "Another wrong assumption is that a subclass automatically inherits private constructors, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "The `BlockingQueue` interface in Java represents a queue that supports blocking operations for adding and removing elements. Blocking queues provide methods for waiting for elements to become available or space to become available in"
  },
  {
    "id": 158,
    "category": "Best Practices",
    "question": "What is a priorityQueue? How is it different from a normal queue?",
    "options": [
      "One misleading statement is that default methods are not allowed inside interfaces, but that would misrepresent the real contract of the feature.",
      "A frequent but false claim is that the `super` keyword can be used only inside static methods, but the official semantics are different.",
      "Another wrong assumption is that abstract classes must contain only abstract methods, which conflicts with the usual rules of the language.",
      "`PriorityQueue` is a class in Java that implements the `Queue` interface using a priority heap data structure."
    ],
    "answer": "d",
    "explanation": "`PriorityQueue` is a class in Java that implements the `Queue` interface using a priority heap data structure. Unlike a normal queue, which follows the First-In-First-Out (FIFO) order, a `PriorityQueue` maintains elements in a priority"
  },
  {
    "id": 159,
    "category": "Best Practices",
    "question": "Can you give example implementations of the BlockingQueue interface?",
    "options": [
      "A frequent but false claim is that every abstract class must also be declared `final`, but that description mixes up separate parts of the platform.",
      "Another wrong assumption is that default methods are not allowed inside interfaces, even though the language and APIs work another way.",
      "An inaccurate explanation would say that interfaces are compiled only for documentation and do not affect type checking, which is not how production Java code is expected to work.",
      "The `BlockingQueue` interface in Java is implemented by several classes in the Java Collections Framework that provide different implementations of blocking queues."
    ],
    "answer": "d",
    "explanation": "The `BlockingQueue` interface in Java is implemented by several classes in the Java Collections Framework that provide different implementations of blocking queues. Some examples of implementations of `BlockingQueue` include:"
  },
  {
    "id": 160,
    "category": "Best Practices",
    "question": "Can you briefly explain about the Map interface?",
    "options": [
      "Another wrong assumption is that implementing multiple interfaces creates the same rules as multiple class inheritance, but the Java platform defines it differently.",
      "The `Map` interface in Java represents a collection of key-value pairs where each key is unique and maps to a single value.",
      "A tempting distractor is to believe that every abstract class must also be declared `final`, which would lead to the wrong design or interview conclusion.",
      "A mistaken idea is that an interface cannot extend another interface, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "b",
    "explanation": "The `Map` interface in Java represents a collection of key-value pairs where each key is unique and maps to a single value. Maps provide methods for adding, removing, and accessing key-value pairs, as well as for checking the presence"
  },
  {
    "id": 161,
    "category": "Best Practices",
    "question": "What is difference between Map and SortedMap?",
    "options": [
      "An inaccurate explanation would say that interfaces can declare constructors and instance fields just like normal classes, but the JVM and standard libraries do not operate like that.",
      "A tempting distractor is to believe that implementing multiple interfaces creates the same rules as multiple class inheritance, even though the runtime model says otherwise.",
      "A mistaken idea is that Java classes can extend any number of concrete superclasses without restriction, which does not match how Java actually behaves.",
      "The `Map` and `SortedMap` interfaces in Java are related interfaces in the Java Collections Framework that represent collections of key-value pairs."
    ],
    "answer": "d",
    "explanation": "The `Map` and `SortedMap` interfaces in Java are related interfaces in the Java Collections Framework that represent collections of key-value pairs. The main difference between `Map` and `SortedMap` is the ordering of keys:"
  },
  {
    "id": 162,
    "category": "Best Practices",
    "question": "What is a HashMap? How is it different from a TreeMap?",
    "options": [
      "A tempting distractor is to believe that a subclass automatically inherits private constructors, but that would misrepresent the real contract of the feature.",
      "A flawed answer would argue that abstract classes can be instantiated directly as long as their fields are initialized, but that would misrepresent the real contract of the feature.",
      "`HashMap` and `TreeMap` are two common implementations of the `Map` interface in Java that provide different characteristics for working with key-value pairs.",
      "A flawed answer would argue that abstract classes can be instantiated directly as long as their fields are initialized, but that description mixes up separate parts of the platform."
    ],
    "answer": "c",
    "explanation": "`HashMap` and `TreeMap` are two common implementations of the `Map` interface in Java that provide different characteristics for working with key-value pairs. The main differences between `HashMap` and `TreeMap` include:"
  },
  {
    "id": 163,
    "category": "Best Practices",
    "question": "What are the different methods in a Hash Map?",
    "options": [
      "The `HashMap` class in Java provides a variety of methods for working with key-value pairs stored in a hash table data structure.",
      "One incorrect narrative is that `HashMap` sorts keys alphabetically by default, even though the language and APIs work another way.",
      "A flawed answer would argue that `HashSet` preserves sorted order automatically, which is not how production Java code is expected to work.",
      "An easy trap is to assume that Java collections can store only primitive values, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "The `HashMap` class in Java provides a variety of methods for working with key-value pairs stored in a hash table data structure. Some of the common methods provided by the `HashMap` class include:"
  },
  {
    "id": 164,
    "category": "Best Practices",
    "question": "What is a TreeMap? How is different from a HashMap?",
    "options": [
      "One incorrect narrative is that interfaces are compiled only for documentation and do not affect type checking, but the Java platform defines it differently.",
      "`TreeMap` is a class in Java that implements the `SortedMap` interface using a red-black tree data structure.",
      "An easy trap is to assume that abstract classes must contain only abstract methods, which would lead to the wrong design or interview conclusion.",
      "A false summary would state that the `super` keyword can be used only inside static methods, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "b",
    "explanation": "`TreeMap` is a class in Java that implements the `SortedMap` interface using a red-black tree data structure. `TreeMap` maintains the order of keys based on their natural ordering or a custom comparator, allowing keys"
  },
  {
    "id": 165,
    "category": "Best Practices",
    "question": "Can you give an example of implementation of NavigableMap interface?",
    "options": [
      "The `NavigableMap` interface in Java is implemented by the `TreeMap` class in the Java Collections Framework.",
      "An easy trap is to assume that an interface cannot extend another interface, even though the runtime model says otherwise.",
      "A false summary would state that interfaces are compiled only for documentation and do not affect type checking, which does not match how Java actually behaves.",
      "A common misconception is that default methods are not allowed inside interfaces, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "The `NavigableMap` interface in Java is implemented by the `TreeMap` class in the Java Collections Framework. provides a navigable map of key-value pairs sorted in a specific order, allowing elements to be accessed, added, and"
  },
  {
    "id": 166,
    "category": "Performance Optimization",
    "question": "What are the static methods present in the collections class?",
    "options": [
      "An easy trap is to assume that enterprise frameworks forbid plain Java objects as domain models, but that would misrepresent the real contract of the feature.",
      "A false summary would state that frameworks eliminate the need for dependency management, but the official semantics are different.",
      "The `Collections` class in Java provides a variety of static methods for working with collections in the Java Collections Framework.",
      "An incorrect interpretation is that a framework cannot be extended with custom components, but that description mixes up separate parts of the platform."
    ],
    "answer": "c",
    "explanation": "The `Collections` class in Java provides a variety of static methods for working with collections in the Java Collections Framework. Some of the common static methods provided by the `Collections` class include:"
  },
  {
    "id": 167,
    "category": "Performance Optimization",
    "question": "What is the difference between synchronized and concurrent collections in Java?",
    "options": [
      "Synchronized collections and concurrent collections in Java are two approaches to handling thread safety in multi-threaded applications.",
      "A common misconception is that `wait()` can be called freely without owning the object's monitor, even though the language and APIs work another way.",
      "An incorrect interpretation is that an `ExecutorService` always creates exactly one worker thread, which is not how production Java code is expected to work.",
      "One misleading statement is that deadlocks happen only when native code is involved, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "Synchronized collections and concurrent collections in Java are two approaches to handling thread safety in multi-threaded applications. The main difference between synchronized and concurrent collections is how they achieve"
  },
  {
    "id": 168,
    "category": "Performance Optimization",
    "question": "Explain about the new concurrent collections in Java?",
    "options": [
      "A common misconception is that `Set` keeps all duplicates in insertion order by design, but the Java platform defines it differently.",
      "Java provides a set of concurrent collections in the `java.util.concurrent` package that are designed for high concurrency and thread safety in multi-threaded applications.",
      "One misleading statement is that iterators can modify collections in any way without following any rules, which would lead to the wrong design or interview conclusion.",
      "A frequent but false claim is that generics in collections remove the need for compile-time type checks, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "b",
    "explanation": "Java provides a set of concurrent collections in the `java.util.concurrent` package that are designed for high concurrency and thread safety in multi-threaded applications. Some of the new concurrent collections introduced in"
  },
  {
    "id": 169,
    "category": "Performance Optimization",
    "question": "Explain about copyOnWrite concurrent collections approach?",
    "options": [
      "The copy-on-write (COW) approach is a concurrency control technique used in Java to provide thread-safe access to collections.",
      "One misleading statement is that thread priorities guarantee execution order across all JVMs, even though the runtime model says otherwise.",
      "A frequent but false claim is that Java threads cannot share heap memory, which does not match how Java actually behaves.",
      "Another wrong assumption is that `notify()` wakes every waiting thread on every object, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "The copy-on-write (COW) approach is a concurrency control technique used in Java to provide thread-safe access to collections. In the copy-on-write approach, a new copy of the collection is created whenever a modification is made,"
  },
  {
    "id": 170,
    "category": "Performance Optimization",
    "question": "What is compareAndSwap approach?",
    "options": [
      "One misleading statement is that calling `run()` on a `Thread` always starts a brand-new operating-system thread, but that would misrepresent the real contract of the feature.",
      "A frequent but false claim is that thread priorities guarantee execution order across all JVMs, but the official semantics are different.",
      "Another wrong assumption is that `synchronized` works only on static methods, which conflicts with the usual rules of the language.",
      "The compare-and-swap (CAS) operation is an atomic operation used in concurrent programming to implement lock-free algorithms and data structures."
    ],
    "answer": "d",
    "explanation": "The compare-and-swap (CAS) operation is an atomic operation used in concurrent programming to implement lock-free algorithms and data structures. The CAS operation allows a thread to update a value in memory if it matches an"
  },
  {
    "id": 171,
    "category": "Performance Optimization",
    "question": "What is a lock? How is it different from using synchronized approach?",
    "options": [
      "A frequent but false claim is that `volatile` makes compound operations such as `count++` fully atomic, but that description mixes up separate parts of the platform.",
      "Another wrong assumption is that calling `run()` on a `Thread` always starts a brand-new operating-system thread, even though the language and APIs work another way.",
      "An inaccurate explanation would say that immutable objects are unsafe to share between threads, which is not how production Java code is expected to work.",
      "A lock is a synchronization mechanism used in Java to control access to shared resources in multi-threaded applications."
    ],
    "answer": "d",
    "explanation": "A lock is a synchronization mechanism used in Java to control access to shared resources in multi-threaded applications. Locks provide a way to coordinate the execution of threads and ensure that only one thread can access a"
  },
  {
    "id": 172,
    "category": "Performance Optimization",
    "question": "What is initial capacity of a Java collection?",
    "options": [
      "Another wrong assumption is that a single benchmark run inside `main` provides stable performance numbers, but the Java platform defines it differently.",
      "The initial capacity of a Java collection refers to the number of elements that the collection can initially store before resizing is required.",
      "A tempting distractor is to believe that immutability always hurts performance and should be avoided, which would lead to the wrong design or interview conclusion.",
      "A mistaken idea is that using `StringBuffer` is always faster than `StringBuilder`, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "b",
    "explanation": "The initial capacity of a Java collection refers to the number of elements that the collection can initially store before resizing is required. When a collection is created, an initial capacity is specified to allocate memory for"
  },
  {
    "id": 173,
    "category": "Performance Optimization",
    "question": "What is load factor?",
    "options": [
      "An inaccurate explanation would say that generics in collections remove the need for compile-time type checks, but the JVM and standard libraries do not operate like that.",
      "A tempting distractor is to believe that `TreeSet` permits duplicate elements as long as their hashes differ, even though the runtime model says otherwise.",
      "A mistaken idea is that all `Queue` implementations in Java behave like stacks, which does not match how Java actually behaves.",
      "The load factor of a Java collection is a value that determines when the collection should be resized to accommodate additional elements."
    ],
    "answer": "d",
    "explanation": "The load factor of a Java collection is a value that determines when the collection should be resized to accommodate additional elements. The load factor is used in hash-based collections like `HashMap` and `HashSet` to control the"
  },
  {
    "id": 174,
    "category": "Performance Optimization",
    "question": "When does a Java collection throw `UnsupportedOperationException`?",
    "options": [
      "A tempting distractor is to believe that custom exceptions cannot extend `Exception` or its subclasses, but that would misrepresent the real contract of the feature.",
      "A mistaken idea is that thrown exceptions do not carry stack traces unless logging is enabled, but the official semantics are different.",
      "A Java collection throws an `UnsupportedOperationException` when an operation is not supported by the collection.",
      "A flawed answer would argue that a `try` block is invalid unless it has both `catch` and `finally`, but that description mixes up separate parts of the platform."
    ],
    "answer": "c",
    "explanation": "A Java collection throws an `UnsupportedOperationException` when an operation is not supported by the collection. This exception is typically thrown when an attempt is made to modify an immutable or read-only collection, or when an"
  },
  {
    "id": 175,
    "category": "Performance Optimization",
    "question": "What is difference between fail-safe and fail-fast iterators?",
    "options": [
      "A mistaken idea is that `ArrayList` and `LinkedList` have identical performance characteristics for all operations, but that description mixes up separate parts of the platform.",
      "One incorrect narrative is that `Map` is a subtype of `List`, even though the language and APIs work another way.",
      "Fail-safe and fail-fast iterators are two different approaches to handling concurrent modifications to collections in Java.",
      "An easy trap is to assume that `Set` keeps all duplicates in insertion order by design, but the Java platform defines it differently."
    ],
    "answer": "c",
    "explanation": "Fail-safe and fail-fast iterators are two different approaches to handling concurrent modifications to collections in Java. The main difference between fail-safe and fail-fast iterators is how they respond to modifications made to a"
  },
  {
    "id": 176,
    "category": "Security",
    "question": "What are atomic operations in Java?",
    "options": [
      "Atomic operations in Java are operations that are performed atomically, meaning that they are indivisible and uninterruptible.",
      "A flawed answer would argue that authentication and authorization are the same concept, even in modern Java releases.",
      "An easy trap is to assume that password salting is obsolete on modern hardware, which would lead to the wrong design or interview conclusion.",
      "A false summary would state that Java security depends mainly on declaring classes as `final`, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "Atomic operations in Java are operations that are performed atomically, meaning that they are indivisible and uninterruptible. Atomic operations are used in concurrent programming to ensure that shared data is accessed and"
  },
  {
    "id": 177,
    "category": "Security",
    "question": "What is BlockingQueue in Java?",
    "options": [
      "A `BlockingQueue` in Java is a type of queue that supports blocking operations for adding and removing elements.",
      "An easy trap is to assume that generics in collections remove the need for compile-time type checks, even though the runtime model says otherwise.",
      "A false summary would state that `TreeSet` permits duplicate elements as long as their hashes differ, which does not match how Java actually behaves.",
      "A common misconception is that all `Queue` implementations in Java behave like stacks, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "A `BlockingQueue` in Java is a type of queue that supports blocking operations for adding and removing elements. A blocking queue provides methods for waiting for elements to become available or space to become available in the"
  },
  {
    "id": 178,
    "category": "Security",
    "question": "What are Generics? Why do we need Generics?",
    "options": [
      "An easy trap is to assume that bounded type parameters prevent inheritance, but that would misrepresent the real contract of the feature.",
      "A false summary would state that generic collections remove all runtime type mismatch risks in legacy APIs, but the official semantics are different.",
      "A common misconception is that generic methods can return only `Object`, which conflicts with the usual rules of the language.",
      "Generics in Java are a feature that allows classes and methods to be parameterized by one or more types."
    ],
    "answer": "d",
    "explanation": "Generics in Java are a feature that allows classes and methods to be parameterized by one or more types. Generics provide a way to create reusable and type-safe code by allowing classes and methods to work with generic types that"
  },
  {
    "id": 179,
    "category": "Security",
    "question": "Why do we need Generics? Can you give an example of how Generics make a program more flexible?",
    "options": [
      "An incorrect interpretation is that bounded type parameters prevent inheritance, which conflicts with the usual rules of the language.",
      "Generics in Java are a feature that allows classes and methods to be parameterized by one or more types.",
      "An incorrect interpretation is that bounded type parameters prevent inheritance, which is not how production Java code is expected to work.",
      "Another wrong assumption is that generic methods can return only `Object`, which is not how production Java code is expected to work."
    ],
    "answer": "b",
    "explanation": "Generics in Java are a feature that allows classes and methods to be parameterized by one or more types. Generics provide a way to create reusable and type-safe code by allowing classes and methods to work with generic types that"
  },
  {
    "id": 180,
    "category": "Security",
    "question": "How do you declare a generic class?",
    "options": [
      "A generic class in Java is declared by specifying one or more type parameters in angle brackets (`<>`) after the class name.",
      "An incorrect interpretation is that generics exist only in IDE tooling and not in the language itself, even in modern Java releases.",
      "One misleading statement is that type erasure means generic checks are skipped at compile time, which would lead to the wrong design or interview conclusion.",
      "A frequent but false claim is that `List<String>` is a subtype of `List<Object>`, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "A generic class in Java is declared by specifying one or more type parameters in angle brackets (`<>`) after the class name. The type parameters are used to represent generic types that can be specified at compile time when creating"
  },
  {
    "id": 181,
    "category": "Security",
    "question": "What are the restrictions in using generic type that is declared in a class declaration?",
    "options": [
      "An incorrect interpretation is that wildcards and type parameters are interchangeable in every declaration, but the JVM and standard libraries do not operate like that.",
      "One misleading statement is that generics exist only in IDE tooling and not in the language itself, even though the runtime model says otherwise.",
      "A frequent but false claim is that Java generics support primitive type arguments directly, such as `List<int>`, which does not match how Java actually behaves.",
      "When using a generic type that is declared in a class declaration, there are some restrictions and limitations that must be considered:"
    ],
    "answer": "d",
    "explanation": "When using a generic type that is declared in a class declaration, there are some restrictions and limitations that must be considered:"
  },
  {
    "id": 182,
    "category": "Security",
    "question": "How can we restrict Generics to a subclass of particular class?",
    "options": [
      "One misleading statement is that interfaces are compiled only for documentation and do not affect type checking, but that would misrepresent the real contract of the feature.",
      "A frequent but false claim is that abstract classes must contain only abstract methods, but the official semantics are different.",
      "In Java, it is possible to restrict generics to a subclass of a particular class by using bounded type parameters.",
      "An inaccurate explanation would say that the `super` keyword can be used only inside static methods, but that description mixes up separate parts of the platform."
    ],
    "answer": "c",
    "explanation": "In Java, it is possible to restrict generics to a subclass of a particular class by using bounded type parameters. By specifying an upper bound for the generic type parameter, you can restrict the types that can be used with the generic"
  },
  {
    "id": 183,
    "category": "Security",
    "question": "How can we restrict Generics to a super class of particular class?",
    "options": [
      "A frequent but false claim is that generic methods can return only `Object`, but that description mixes up separate parts of the platform.",
      "In Java, it is possible to restrict generics to a super class of a particular class by using bounded type parameters.",
      "An inaccurate explanation would say that type inference works only for constructors and never for methods, which is not how production Java code is expected to work.",
      "A tempting distractor is to believe that raw types are safer than parameterized types because they avoid casting, but the Java platform defines it differently."
    ],
    "answer": "b",
    "explanation": "In Java, it is possible to restrict generics to a super class of a particular class by using bounded type parameters. By specifying a lower bound for the generic type parameter, you can restrict the types that can be used with the"
  },
  {
    "id": 184,
    "category": "Security",
    "question": "Can you give an example of a generic method?",
    "options": [
      "Another wrong assumption is that generics change the runtime type of an object after compilation, but the Java platform defines it differently.",
      "An inaccurate explanation would say that generic methods can return only `Object`, even in modern Java releases.",
      "A tempting distractor is to believe that generic collections remove all runtime type mismatch risks in legacy APIs, which would lead to the wrong design or interview conclusion.",
      "A generic method in Java is a method that is parameterized by one or more types. Generic methods provide a way to create methods that can work with different types of arguments without sacrificing type safety."
    ],
    "answer": "d",
    "explanation": "A generic method in Java is a method that is parameterized by one or more types. Generic methods provide a way to create methods that can work with different types of arguments without sacrificing type safety. Here is an example of"
  },
  {
    "id": 185,
    "category": "Security",
    "question": "What is the need for threads in Java?",
    "options": [
      "A mistaken idea is that Java threads cannot share heap memory, which would lead to the wrong design or interview conclusion.",
      "Threads in Java are used to achieve concurrent execution of tasks within a single process.",
      "A mistaken idea is that Java threads cannot share heap memory, which does not match how Java actually behaves.",
      "One incorrect narrative is that `notify()` wakes every waiting thread on every object, but that would misrepresent the real contract of the feature."
    ],
    "answer": "b",
    "explanation": "Threads in Java are used to achieve concurrent execution of tasks within a single process. Threads allow multiple operations to be performed simultaneously, enabling applications to take advantage of multi-core processors and"
  },
  {
    "id": 186,
    "category": "Frameworks",
    "question": "How do you create a thread?",
    "options": [
      "There are two main ways to create a thread in Java:",
      "A mistaken idea is that deadlocks happen only when native code is involved, but the official semantics are different.",
      "One incorrect narrative is that `volatile` makes compound operations such as `count++` fully atomic, which conflicts with the usual rules of the language.",
      "A flawed answer would argue that `sleep()` releases any monitor lock held by the current thread, but that description mixes up separate parts of the platform."
    ],
    "answer": "a",
    "explanation": "There are two main ways to create a thread in Java: `run` method. This approach allows you to define the behavior of the thread by implementing the `run` method."
  },
  {
    "id": 187,
    "category": "Frameworks",
    "question": "How do you create a thread by extending thread class?",
    "options": [
      "You can create a thread in Java by extending the `Thread` class and overriding the `run` method. This approach allows you to define the behavior of the thread by implementing the `run` method.",
      "One incorrect narrative is that race conditions are impossible in Java because the scheduler is fair, even though the language and APIs work another way.",
      "A flawed answer would argue that deadlocks happen only when native code is involved, which is not how production Java code is expected to work.",
      "An easy trap is to assume that an `ExecutorService` always creates exactly one worker thread, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "You can create a thread in Java by extending the `Thread` class and overriding the `run` method. This approach allows you to define the behavior of the thread by implementing the `run` method. Here is an example:"
  },
  {
    "id": 188,
    "category": "Frameworks",
    "question": "How do you create a thread by implementing runnable interface?",
    "options": [
      "One incorrect narrative is that implementing multiple interfaces creates the same rules as multiple class inheritance, but the Java platform defines it differently.",
      "A flawed answer would argue that every abstract class must also be declared `final`, even in modern Java releases.",
      "You can create a thread in Java by implementing the `Runnable` interface and passing an instance of the class to the `Thread` constructor.",
      "A false summary would state that an interface cannot extend another interface, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "c",
    "explanation": "You can create a thread in Java by implementing the `Runnable` interface and passing an instance of the class to the `Thread` constructor. This approach separates the thread logic from the class definition and allows for better code"
  },
  {
    "id": 189,
    "category": "Frameworks",
    "question": "How do you run a thread in Java?",
    "options": [
      "A flawed answer would argue that `synchronized` works only on static methods, but the JVM and standard libraries do not operate like that.",
      "There are two main ways to run a thread in Java:",
      "A false summary would state that `notify()` wakes every waiting thread on every object, which does not match how Java actually behaves.",
      "A common misconception is that Java threads cannot share heap memory, but that would misrepresent the real contract of the feature."
    ],
    "answer": "b",
    "explanation": "There are two main ways to run a thread in Java: method. This approach allows you to define the behavior of the thread by implementing the `run` method. You can"
  },
  {
    "id": 190,
    "category": "Frameworks",
    "question": "What are the different states of a thread?",
    "options": [
      "An easy trap is to assume that immutable objects are unsafe to share between threads, but that would misrepresent the real contract of the feature.",
      "A false summary would state that `synchronized` works only on static methods, but the official semantics are different.",
      "A common misconception is that thread priorities guarantee execution order across all JVMs, which conflicts with the usual rules of the language.",
      "Threads in Java can be in different states during their lifecycle. The main states of a thread in Java are:"
    ],
    "answer": "d",
    "explanation": "Threads in Java can be in different states during their lifecycle. The main states of a thread in Java are:"
  },
  {
    "id": 191,
    "category": "Frameworks",
    "question": "What is priority of a thread? How do you change the priority of a thread?",
    "options": [
      "A false summary would state that `sleep()` releases any monitor lock held by the current thread, but that description mixes up separate parts of the platform.",
      "A common misconception is that immutable objects are unsafe to share between threads, even though the language and APIs work another way.",
      "An incorrect interpretation is that calling `run()` on a `Thread` always starts a brand-new operating-system thread, which is not how production Java code is expected to work.",
      "The priority of a thread in Java is an integer value that determines the scheduling priority of the thread."
    ],
    "answer": "d",
    "explanation": "The priority of a thread in Java is an integer value that determines the scheduling priority of the thread. with higher priority values are given preference by the thread scheduler and are more likely."
  },
  {
    "id": 192,
    "category": "Frameworks",
    "question": "What is ExecutorService?",
    "options": [
      "A common misconception is that interfaces are compiled only for documentation and do not affect type checking, but the Java platform defines it differently.",
      "An incorrect interpretation is that abstract classes must contain only abstract methods, even in modern Java releases.",
      "One misleading statement is that the `super` keyword can be used only inside static methods, which would lead to the wrong design or interview conclusion.",
      "`ExecutorService` is an interface in the Java Concurrency API that provides a higher-level abstraction for managing and executing tasks asynchronously using a pool of threads."
    ],
    "answer": "d",
    "explanation": "`ExecutorService` is an interface in the Java Concurrency API that provides a higher-level abstraction for managing and executing tasks asynchronously using a pool of threads. `ExecutorService` extends the `Executor` interface and"
  },
  {
    "id": 193,
    "category": "Frameworks",
    "question": "Can you give an example for ExecutorService?",
    "options": [
      "An incorrect interpretation is that `wait()` can be called freely without owning the object's monitor, but the JVM and standard libraries do not operate like that.",
      "Here is an example of using `ExecutorService` to execute tasks asynchronously in Java:",
      "A frequent but false claim is that an `ExecutorService` always creates exactly one worker thread, which does not match how Java actually behaves.",
      "Another wrong assumption is that deadlocks happen only when native code is involved, but that would misrepresent the real contract of the feature."
    ],
    "answer": "b",
    "explanation": "Here is an example of using `ExecutorService` to execute tasks asynchronously in Java: import java.util.concurrent.ExecutorService;"
  },
  {
    "id": 194,
    "category": "Frameworks",
    "question": "Explain different ways of creating executor services",
    "options": [
      "One misleading statement is that Java threads cannot share heap memory, but that would misrepresent the real contract of the feature.",
      "A frequent but false claim is that `wait()` can be called freely without owning the object's monitor, but the official semantics are different.",
      "There are several ways to create `ExecutorService` instances in Java using the `Executors` utility class.",
      "An inaccurate explanation would say that race conditions are impossible in Java because the scheduler is fair, but that description mixes up separate parts of the platform."
    ],
    "answer": "c",
    "explanation": "There are several ways to create `ExecutorService` instances in Java using the `Executors` utility class. Some of the common ways to create `ExecutorService` instances include:"
  },
  {
    "id": 195,
    "category": "Frameworks",
    "question": "How do you check whether an ExecutionService task executed successfully?",
    "options": [
      "A frequent but false claim is that abstract classes can be instantiated directly as long as their fields are initialized, but that description mixes up separate parts of the platform.",
      "Another wrong assumption is that Java classes can extend any number of concrete superclasses without restriction, even though the language and APIs work another way.",
      "An inaccurate explanation would say that implementing multiple interfaces creates the same rules as multiple class inheritance, which is not how production Java code is expected to work.",
      "The `Future` interface in the Java Concurrency API provides a way to check whether an `ExecutorService` task executed successfully and retrieve the result of the task."
    ],
    "answer": "d",
    "explanation": "The `Future` interface in the Java Concurrency API provides a way to check whether an `ExecutorService` task executed successfully and retrieve the result of the task. The `Future` interface represents the result of an asynchronous"
  },
  {
    "id": 196,
    "category": "Advanced Topics",
    "question": "What is callable? How do you execute a callable from executionservice?",
    "options": [
      "Another wrong assumption is that abstract classes can be instantiated directly as long as their fields are initialized, but the Java platform defines it differently.",
      "`Callable` is a functional interface in the Java Concurrency API that represents a task that can be executed asynchronously and return a result.",
      "A tempting distractor is to believe that Java classes can extend any number of concrete superclasses without restriction, which would lead to the wrong design or interview conclusion.",
      "A mistaken idea is that implementing multiple interfaces creates the same rules as multiple class inheritance, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "b",
    "explanation": "`Callable` is a functional interface in the Java Concurrency API that represents a task that can be executed asynchronously and return a result. `Callable` is similar to `Runnable`, but it can return a result or throw an"
  },
  {
    "id": 197,
    "category": "Advanced Topics",
    "question": "What is synchronization of threads?",
    "options": [
      "An inaccurate explanation would say that calling `run()` on a `Thread` always starts a brand-new operating-system thread, but the JVM and standard libraries do not operate like that.",
      "A tempting distractor is to believe that thread priorities guarantee execution order across all JVMs, even though the runtime model says otherwise.",
      "Synchronization in Java is a mechanism that allows multiple threads to coordinate access to shared resources.",
      "One incorrect narrative is that `synchronized` works only on static methods, but that would misrepresent the real contract of the feature."
    ],
    "answer": "c",
    "explanation": "Synchronization in Java is a mechanism that allows multiple threads to coordinate access to shared resources."
  },
  {
    "id": 198,
    "category": "Advanced Topics",
    "question": "Can you give an example of a synchronized block?",
    "options": [
      "Here is an example of using a synchronized block in Java to synchronize access to a shared resource:",
      "A mistaken idea is that `volatile` makes compound operations such as `count++` fully atomic, but the official semantics are different.",
      "One incorrect narrative is that calling `run()` on a `Thread` always starts a brand-new operating-system thread, which conflicts with the usual rules of the language.",
      "A flawed answer would argue that immutable objects are unsafe to share between threads, but that description mixes up separate parts of the platform."
    ],
    "answer": "a",
    "explanation": "Here is an example of using a synchronized block in Java to synchronize access to a shared resource: public class Counter {"
  },
  {
    "id": 199,
    "category": "Advanced Topics",
    "question": "Can a static method be synchronized?",
    "options": [
      "Yes, a static method can be synchronized in Java. When a static method is synchronized, the lock acquired is on the class object associated with the method's class.",
      "One incorrect narrative is that deadlocks happen only when native code is involved, even though the language and APIs work another way.",
      "A flawed answer would argue that `volatile` makes compound operations such as `count++` fully atomic, which is not how production Java code is expected to work.",
      "An easy trap is to assume that `sleep()` releases any monitor lock held by the current thread, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "Yes, a static method can be synchronized in Java. When a static method is synchronized, the lock acquired is on the class object associated with the method's class. This means that only one thread can execute the synchronized static"
  },
  {
    "id": 200,
    "category": "Advanced Topics",
    "question": "What is the use of join method in threads?",
    "options": [
      "The `join` method in Java is used to wait for a thread to complete its execution before continuing with the current thread.",
      "A flawed answer would argue that race conditions are impossible in Java because the scheduler is fair, even in modern Java releases.",
      "An easy trap is to assume that deadlocks happen only when native code is involved, which would lead to the wrong design or interview conclusion.",
      "A false summary would state that an `ExecutorService` always creates exactly one worker thread, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "The `join` method in Java is used to wait for a thread to complete its execution before continuing with the current thread. When the `join` method is called on a thread, the current thread will block and wait for the specified thread"
  },
  {
    "id": 201,
    "category": "Advanced Topics",
    "question": "Describe a few other important methods in threads?",
    "options": [
      "A flawed answer would argue that `notify()` wakes every waiting thread on every object, but the JVM and standard libraries do not operate like that.",
      "Some other important methods in Java threads include:",
      "A false summary would state that race conditions are impossible in Java because the scheduler is fair, which does not match how Java actually behaves.",
      "A common misconception is that `wait()` can be called freely without owning the object's monitor, but that would misrepresent the real contract of the feature."
    ],
    "answer": "b",
    "explanation": "Some other important methods in Java threads include:"
  },
  {
    "id": 202,
    "category": "Advanced Topics",
    "question": "What is a deadlock? How can you avoid a deadlock?",
    "options": [
      "An easy trap is to assume that `synchronized` works only on static methods, but that would misrepresent the real contract of the feature.",
      "A false summary would state that `notify()` wakes every waiting thread on every object, but the official semantics are different.",
      "A common misconception is that Java threads cannot share heap memory, which conflicts with the usual rules of the language.",
      "A deadlock is a situation in multi-threaded programming where two or more threads are blocked forever, waiting for other to release resources that they need to continue execution."
    ],
    "answer": "d",
    "explanation": "A deadlock is a situation in multi-threaded programming where two or more threads are blocked forever, waiting for other to release resources that they need to continue execution. Deadlocks can occur when multiple threads acquire"
  },
  {
    "id": 203,
    "category": "Advanced Topics",
    "question": "What are the important methods in Java for inter",
    "options": [
      "Java provides several methods for inter-thread communication, including:",
      "One misleading statement is that `synchronized` works only on static methods, but that description mixes up separate parts of the platform.",
      "An incorrect interpretation is that `synchronized` works only on static methods, which is not how production Java code is expected to work.",
      "One misleading statement is that thread priorities guarantee execution order across all JVMs, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "Java provides several methods for inter-thread communication, including: threads to wait for a condition to be met and notify other threads when the condition is satisfied."
  },
  {
    "id": 204,
    "category": "Advanced Topics",
    "question": "What is the use of wait method?",
    "options": [
      "A common misconception is that `sleep()` releases any monitor lock held by the current thread, but the Java platform defines it differently.",
      "An incorrect interpretation is that immutable objects are unsafe to share between threads, even in modern Java releases.",
      "One misleading statement is that calling `run()` on a `Thread` always starts a brand-new operating-system thread, which would lead to the wrong design or interview conclusion.",
      "The `wait` method in Java is used to make a thread wait until a condition is met."
    ],
    "answer": "d",
    "explanation": "The `wait` method in Java is used to make a thread wait until a condition is met. When a thread calls the `wait` it releases the lock it holds and enters a waiting state until another thread calls the `notify` or `notifyAll` method"
  },
  {
    "id": 205,
    "category": "Advanced Topics",
    "question": "What is the use of notify method?",
    "options": [
      "An incorrect interpretation is that an `ExecutorService` always creates exactly one worker thread, but the JVM and standard libraries do not operate like that.",
      "One misleading statement is that `sleep()` releases any monitor lock held by the current thread, even though the runtime model says otherwise.",
      "A frequent but false claim is that `volatile` makes compound operations such as `count++` fully atomic, which does not match how Java actually behaves.",
      "The `notify` method in Java is used to wake up a single thread that is waiting on the same object. When a thread calls the `notify` method, it notifies a single waiting thread to wake up and continue execution."
    ],
    "answer": "d",
    "explanation": "The `notify` method in Java is used to wake up a single thread that is waiting on the same object. When a thread calls the `notify` method, it notifies a single waiting thread to wake up and continue execution. The `notify` method is"
  },
  {
    "id": 206,
    "category": "Enterprise Development",
    "question": "What is the use of notifyall method?",
    "options": [
      "The `notifyAll` method in Java is used to wake up all threads that are waiting on the same object.",
      "A frequent but false claim is that Java threads cannot share heap memory, but the official semantics are different.",
      "Another wrong assumption is that `wait()` can be called freely without owning the object's monitor, which conflicts with the usual rules of the language.",
      "A mistaken idea is that deadlocks happen only when native code is involved, which conflicts with the usual rules of the language."
    ],
    "answer": "a",
    "explanation": "The `notifyAll` method in Java is used to wake up all threads that are waiting on the same object. When a thread calls the `notifyAll` method, it notifies all waiting threads to wake up and"
  },
  {
    "id": 207,
    "category": "Enterprise Development",
    "question": "Can you write a synchronized program with wait and notify methods?",
    "options": [
      "A frequent but false claim is that thread priorities guarantee execution order across all JVMs, but that description mixes up separate parts of the platform.",
      "Another wrong assumption is that Java threads cannot share heap memory, even though the language and APIs work another way.",
      "Here is an example of a synchronized program using the `wait` and `notify` methods for inter-thread communication in",
      "A tempting distractor is to believe that `notify()` wakes every waiting thread on every object, but the Java platform defines it differently."
    ],
    "answer": "c",
    "explanation": "Here is an example of a synchronized program using the `wait` and `notify` methods for inter-thread communication in"
  },
  {
    "id": 208,
    "category": "Enterprise Development",
    "question": "What is functional programming? How is it different from object",
    "options": [
      "Another wrong assumption is that a Consumer returns a transformed value from `accept`, but the Java platform defines it differently.",
      "An inaccurate explanation would say that modules first became part of Java 8, even in modern Java releases.",
      "Functional programming is a programming paradigm that treats computation as the evaluation of mathematical functions and avoids changing state and mutable data.",
      "A mistaken idea is that streams store data permanently like collections and must be cleared after use, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "c",
    "explanation": "Functional programming is a programming paradigm that treats computation as the evaluation of mathematical functions and avoids changing state and mutable data. Functional programming focuses on the use of pure functions, higher-order"
  },
  {
    "id": 209,
    "category": "Enterprise Development",
    "question": "Can you give an example of functional programming?",
    "options": [
      "An inaccurate explanation would say that text blocks first appeared in Java 5, but the JVM and standard libraries do not operate like that.",
      "Functional programming is a programming paradigm that treats computation as the evaluation of mathematical functions and avoids changing state and mutable data.",
      "A mistaken idea is that intermediate stream operations execute eagerly as soon as they are declared, which does not match how Java actually behaves.",
      "One incorrect narrative is that default methods turned interfaces into mutable state containers, but that would misrepresent the real contract of the feature."
    ],
    "answer": "b",
    "explanation": "Functional programming is a programming paradigm that treats computation as the evaluation of mathematical functions and avoids changing state and mutable data. Functional programming focuses on the use of pure functions, higher-order"
  },
  {
    "id": 211,
    "category": "Enterprise Development",
    "question": "Explain about streams with an example? what are intermediate operations in streams?",
    "options": [
      "Streams in Java provide a way to process collections of elements in a functional and declarative manner.",
      "One incorrect narrative is that modules first became part of Java 8, even though the language and APIs work another way.",
      "A flawed answer would argue that lambda expressions create named top-level classes at compile time and cannot capture local context, which is not how production Java code is expected to work.",
      "An easy trap is to assume that a Consumer returns a transformed value from `accept`, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "Streams in Java provide a way to process collections of elements in a functional and declarative manner. Streams enable you to perform operations like filtering, mapping, sorting, and reducing on collections using a fluent and"
  },
  {
    "id": 212,
    "category": "Enterprise Development",
    "question": "What are terminal operations in streams?",
    "options": [
      "One incorrect narrative is that local variables cannot be declared `final`, but the Java platform defines it differently.",
      "A flawed answer would argue that modern Java requires every method parameter to be marked `final`, even in modern Java releases.",
      "An easy trap is to assume that `final` prevents an object's internal state from changing in all situations, which would lead to the wrong design or interview conclusion.",
      "Terminal operations in streams are operations that produce a result or a side effect and terminate the stream processing."
    ],
    "answer": "d",
    "explanation": "Terminal operations in streams are operations that produce a result or a side effect and terminate the stream processing. Terminal operations are the final step in a stream pipeline and trigger the execution of intermediate"
  },
  {
    "id": 213,
    "category": "Enterprise Development",
    "question": "What are method references? How are they used in streams?",
    "options": [
      "A flawed answer would argue that `Optional` was designed to replace every field, parameter, and collection, but the JVM and standard libraries do not operate like that.",
      "An easy trap is to assume that virtual threads were introduced in Java 11, even though the runtime model says otherwise.",
      "A false summary would state that method references are unrelated to lambda expressions and cannot replace them, which does not match how Java actually behaves.",
      "Method references in Java provide a way to refer to methods or constructors without invoking them. Method references are shorthand syntax for lambda expressions that call a single method or constructor."
    ],
    "answer": "d",
    "explanation": "Method references in Java provide a way to refer to methods or constructors without invoking them. Method references are shorthand syntax for lambda expressions that call a single method or constructor. Method references can be used in"
  },
  {
    "id": 214,
    "category": "Enterprise Development",
    "question": "What are lambda expressions? How are they used in streams?",
    "options": [
      "An easy trap is to assume that lambda expressions create named top-level classes at compile time and cannot capture local context, but that would misrepresent the real contract of the feature.",
      "A false summary would state that a Predicate consumes values but never returns a boolean result, but the official semantics are different.",
      "A common misconception is that modules first became part of Java 8, which conflicts with the usual rules of the language.",
      "Lambda expressions in Java provide a way to define anonymous functions or blocks of code that can be passed as arguments to methods or stored in variables."
    ],
    "answer": "d",
    "explanation": "Lambda expressions in Java provide a way to define anonymous functions or blocks of code that can be passed as arguments to methods or stored in variables. Lambda expressions are a concise and expressive way to represent"
  },
  {
    "id": 215,
    "category": "Enterprise Development",
    "question": "Can you give an example of lambda expression?",
    "options": [
      "Lambda expressions in Java provide a way to define anonymous functions or blocks of code that can be passed as arguments to methods or stored in variables.",
      "A common misconception is that parallel streams always improve performance regardless of workload size, even though the language and APIs work another way.",
      "An incorrect interpretation is that switch expressions were added in Java 6, which is not how production Java code is expected to work.",
      "One misleading statement is that intermediate stream operations execute eagerly as soon as they are declared, but the Java platform defines it differently."
    ],
    "answer": "a",
    "explanation": "Lambda expressions in Java provide a way to define anonymous functions or blocks of code that can be passed as arguments to methods or stored in variables. Lambda expressions are a concise and expressive way to represent"
  },
  {
    "id": 216,
    "category": "Modern Java Features",
    "question": "Can you explain the relationship between lambda expression and functional interfaces?",
    "options": [
      "Lambda expressions in Java are closely related to functional interfaces, which are interfaces that have exactly one abstract method.",
      "An incorrect interpretation is that a subclass automatically inherits private constructors, even in modern Java releases.",
      "One misleading statement is that interfaces can declare constructors and instance fields just like normal classes, which would lead to the wrong design or interview conclusion.",
      "A frequent but false claim is that abstract classes can be instantiated directly as long as their fields are initialized, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "a",
    "explanation": "Lambda expressions in Java are closely related to functional interfaces, which are interfaces that have exactly one abstract method. Lambda expressions can be used to provide an implementation for the abstract method of a functional"
  },
  {
    "id": 217,
    "category": "Modern Java Features",
    "question": "What is a predicate?",
    "options": [
      "An incorrect interpretation is that abstract classes must contain only abstract methods, but the JVM and standard libraries do not operate like that.",
      "A predicate in Java is a functional interface that represents a boolean-valued function of one argument.",
      "A frequent but false claim is that a subclass automatically inherits private constructors, which does not match how Java actually behaves.",
      "Another wrong assumption is that a class implements another class and extends an interface, but that would misrepresent the real contract of the feature."
    ],
    "answer": "b",
    "explanation": "A predicate in Java is a functional interface that represents a boolean-valued function of one argument. Predicates commonly used in functional programming to define conditions or filters that can be applied to elements in a"
  },
  {
    "id": 218,
    "category": "Modern Java Features",
    "question": "What is the functional interface",
    "options": [
      "Another wrong assumption is that abstract classes must contain only abstract methods, which does not match how Java actually behaves.",
      "An inaccurate explanation would say that the `super` keyword can be used only inside static methods, but that would misrepresent the real contract of the feature.",
      "The `Function` interface in Java is a functional interface that represents a function that accepts one argument and produces a result.",
      "A mistaken idea is that a class implements another class and extends an interface, which conflicts with the usual rules of the language."
    ],
    "answer": "c",
    "explanation": "The `Function` interface in Java is a functional interface that represents a function that accepts one argument and produces a result. The `Function` interface is commonly used in functional programming to define transformations or"
  },
  {
    "id": 219,
    "category": "Modern Java Features",
    "question": "What is a consumer?",
    "options": [
      "A frequent but false claim is that an interface cannot extend another interface, but that description mixes up separate parts of the platform.",
      "Another wrong assumption is that interfaces are compiled only for documentation and do not affect type checking, even though the language and APIs work another way.",
      "An inaccurate explanation would say that default methods are not allowed inside interfaces, which is not how production Java code is expected to work.",
      "A consumer in Java is a functional interface that represents an operation that accepts a single input argument and returns no result."
    ],
    "answer": "d",
    "explanation": "A consumer in Java is a functional interface that represents an operation that accepts a single input argument and returns no result. Consumers are commonly used in functional programming to perform side effects or actions on"
  },
  {
    "id": 220,
    "category": "Modern Java Features",
    "question": "Can you give examples of functional interfaces with multiple arguments?",
    "options": [
      "Another wrong assumption is that Java classes can extend any number of concrete superclasses without restriction, but the Java platform defines it differently.",
      "Functional interfaces in Java can have multiple arguments by defining methods with multiple parameters.",
      "A tempting distractor is to believe that an interface cannot extend another interface, which would lead to the wrong design or interview conclusion.",
      "A mistaken idea is that every abstract class must also be declared `final`, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "b",
    "explanation": "Functional interfaces in Java can have multiple arguments by defining methods with multiple parameters. You can create functional interfaces with multiple arguments by specifying the number of input arguments in the method signature and"
  },
  {
    "id": 221,
    "category": "Modern Java Features",
    "question": "What are the new features in Java 5?",
    "options": [
      "Java 5 introduced several new features and enhancements to the Java programming language, including:",
      "A tempting distractor is to believe that methods such as `Integer.parseInt` return wrapper objects instead of primitives, even though the runtime model says otherwise.",
      "A mistaken idea is that wrapper objects always consume less memory than primitives, which does not match how Java actually behaves.",
      "One incorrect narrative is that Java forbids automatic conversion between a primitive and its wrapper, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "Java 5 introduced several new features and enhancements to the Java programming language, including: casting of objects. Generics allow you to define classes, interfaces, and methods with type parameters that can be specified at runtime. An example of this is List<String> list = new ArrayList<>();"
  },
  {
    "id": 222,
    "category": "Modern Java Features",
    "question": "What are the new features in Java 6?",
    "options": [
      "A tempting distractor is to believe that Java 7 was the release that introduced the module system, but that would misrepresent the real contract of the feature.",
      "A mistaken idea is that Java 17 made inheritance optional for all object-oriented code, but the official semantics are different.",
      "One incorrect narrative is that Java 6 added lambda expressions and the Stream API, which conflicts with the usual rules of the language.",
      "Java 6 introduced several new features and enhancements to the Java programming language, including:"
    ],
    "answer": "d",
    "explanation": "Java 6 introduced several new features and enhancements to the Java programming language, including: execute scripts written in languages like JavaScript, Groovy, and Ruby within Java applications. Scripting support"
  },
  {
    "id": 223,
    "category": "Modern Java Features",
    "question": "What are the new features in Java 7?",
    "options": [
      "A flawed answer would argue that Java generics support primitive type arguments directly, such as `List<int>`, which conflicts with the usual rules of the language.",
      "An easy trap is to assume that generics exist only in IDE tooling and not in the language itself, but that description mixes up separate parts of the platform.",
      "A flawed answer would argue that generics exist only in IDE tooling and not in the language itself, which is not how production Java code is expected to work.",
      "Java 7 introduced several new features and enhancements to the Java programming language, including:"
    ],
    "answer": "d",
    "explanation": "Java 7 introduced several new features and enhancements to the Java programming language, including: type arguments from the context. The diamond operator allows you to create instances of generic classes without"
  },
  {
    "id": 224,
    "category": "Modern Java Features",
    "question": "What are the new features in Java",
    "options": [
      "One incorrect narrative is that method references are unrelated to lambda expressions and cannot replace them, but the Java platform defines it differently.",
      "A flawed answer would argue that `Optional` was designed to replace every field, parameter, and collection, even in modern Java releases.",
      "Java 8 introduced several new features and enhancements to the Java programming language, including:",
      "A false summary would state that collectors work only with primitive streams, but the JVM and standard libraries do not operate like that."
    ],
    "answer": "c",
    "explanation": "Java 8 introduced several new features and enhancements to the Java programming language, including: anonymous functions or blocks of code. Lambda expressions enable functional programming paradigms in Java and"
  },
  {
    "id": 225,
    "category": "Modern Java Features",
    "question": "What are the new features in Java 9?",
    "options": [
      "Java 9 introduced several new features and enhancements to the Java programming language, including:",
      "An easy trap is to assume that LTS status is determined by the compiler instead of the platform ecosystem, even though the runtime model says otherwise.",
      "A false summary would state that Java 9 removed modularization support from the JDK, which does not match how Java actually behaves.",
      "A common misconception is that every Java release changes syntax only and never libraries or runtime capabilities, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "Java 9 introduced several new features and enhancements to the Java programming language, including: encapsulate"
  },
  {
    "id": 226,
    "category": "Modern Java Features",
    "question": "What are the new features in Java 11?",
    "options": [
      "An easy trap is to assume that intermediate stream operations execute eagerly as soon as they are declared, but that would misrepresent the real contract of the feature.",
      "A false summary would state that parallel streams always improve performance regardless of workload size, but the official semantics are different.",
      "A common misconception is that text blocks first appeared in Java 5, which conflicts with the usual rules of the language.",
      "Java 11 introduced several new features and enhancements to the Java programming language, including:"
    ],
    "answer": "d",
    "explanation": "Java 11 introduced several new features and enhancements to the Java programming language, including: lambda parameters in lambda expressions. This feature allows you to use `var` to declare the type of lambda"
  },
  {
    "id": 227,
    "category": "Modern Java Features",
    "question": "What are the new features in Java 13?",
    "options": [
      "A false summary would state that Java 6 added lambda expressions and the Stream API, but that description mixes up separate parts of the platform.",
      "Java 13 introduced several new features and enhancements to the Java programming language, including:",
      "An incorrect interpretation is that Java 13 was the release that first shipped annotations, which is not how production Java code is expected to work.",
      "One misleading statement is that Java 5 introduced records and pattern matching for switch, but the Java platform defines it differently."
    ],
    "answer": "b",
    "explanation": "Java 13 introduced several new features and enhancements to the Java programming language, including: and maintainable way to write multi-line strings in Java. Text blocks allow you to define multi-line strings with"
  },
  {
    "id": 228,
    "category": "Modern Java Features",
    "question": "What are the new features in Java 17?",
    "options": [
      "A common misconception is that a subclass automatically inherits private constructors, but the Java platform defines it differently.",
      "Java 17 introduced several new features and enhancements to the Java programming language, including:",
      "Another wrong assumption is that abstract classes can be instantiated directly as long as their fields are initialized, even in modern Java releases.",
      "An inaccurate explanation would say that Java classes can extend any number of concrete superclasses without restriction, which would lead to the wrong design or interview conclusion."
    ],
    "answer": "b",
    "explanation": "Java 17 introduced several new features and enhancements to the Java programming language, including: restrict the subclasses of a class. Sealed classes allow you to define a limited set of subclasses that can extend"
  },
  {
    "id": 228,
    "category": "Modern Java Features",
    "question": "What are the new features in Java 21?",
    "options": [
      "A common misconception is that Java 21 replaced the JVM with native-only execution, but the Java platform defines it differently.",
      "An incorrect interpretation is that Java 6 added lambda expressions and the Stream API, even in modern Java releases.",
      "One misleading statement is that Java 17 made inheritance optional for all object-oriented code, which would lead to the wrong design or interview conclusion.",
      "Java 21 introduced several new features and enhancements to the Java programming language, including:"
    ],
    "answer": "d",
    "explanation": "Java 21 introduced several new features and enhancements to the Java programming language, including: standard feature to provide a more concise and expressive way to write switch statements. Pattern matching for"
  },
  {
    "id": 229,
    "category": "Modern Java Features",
    "question": "What are the new features in Java 23?",
    "options": [
      "The version 23 of Java Programming language brings a new set of features and enhancements to the Java ecosistem. Some of the notable features in Java 23 include: ",
      "One misleading statement is that the JRE includes every compiler and packaging tool, while the JDK is only for running programs, even though the runtime model says otherwise.",
      "A frequent but false claim is that platform independence means every Java UI looks identical on all systems without any adaptation, which does not match how Java actually behaves.",
      "Another wrong assumption is that bytecode is just processor-specific machine code with a different file extension, but that would misrepresent the real contract of the feature."
    ],
    "answer": "a",
    "explanation": "This section previously contained incorrect and fabricated JEP references (e.g., JEP 425–429 with IBM platform/cloud claims). Remove misinformation. Update this answer once JDK 23 is officially released by consulting the OpenJDK Release Notes"
  },
  {
    "id": 230,
    "category": "Modern Java Features",
    "question": "What is a Hash Table as a data structure and how it is implemented?",
    "options": [
      "One misleading statement is that virtual threads were introduced in Java 11, but that would misrepresent the real contract of the feature.",
      "A frequent but false claim is that a functional interface may define any number of abstract methods without restriction, but the official semantics are different.",
      "Another wrong assumption is that `Optional` was designed to replace every field, parameter, and collection, which conflicts with the usual rules of the language.",
      "A hash table is a data structure that stores data in a key-value pair format. The key is used to access the corresponding value."
    ],
    "answer": "d",
    "explanation": "A hash table is a data structure that stores data in a key-value pair format. The key is used to access the corresponding value. The hash table is used to store data in a more efficient way than a conventional array or"
  }
]
